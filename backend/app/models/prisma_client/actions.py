# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class UserActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.User]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await User.prisma().query_raw(
            'SELECT * FROM User WHERE id = ?',
            'bbadfchfja',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.User
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await User.prisma().query_first(
            'SELECT * FROM User WHERE email = ?',
            'dhheabfhf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.UserCreateInput,
        include: Optional[types.UserInclude] = None
    ) -> _PrismaModelT:
        """Create a new User record.

        Parameters
        ----------
        data
            User record data
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The created User record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a User record from just the required fields
        user = await User.prisma().create(
            data={
                # data to create a User record
                'email': 'ggciceaie',
                'passwordHash': 'bbehjachib',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.UserCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple User records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of User record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await User.prisma().create_many(
            data=[
                {
                    # data to create a User record
                    'email': 'cadfabfehe',
                    'passwordHash': 'dgiiaaijj',
                },
                {
                    # data to create a User record
                    'email': 'bfaiacjjfc',
                    'passwordHash': 'eigcfgbif',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.UserWhereUniqueInput,
        include: Optional[types.UserInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single User record.

        Parameters
        ----------
        where
            User filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The deleted User record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = await User.prisma().delete(
            where={
                'id': 'bagcfbhiig',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.UserWhereUniqueInput,
        include: Optional[types.UserInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique User record.

        Parameters
        ----------
        where
            User filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The found User record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = await User.prisma().find_unique(
            where={
                'id': 'cghideieh',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.UserWhereUniqueInput,
        include: Optional[types.UserInclude] = None
    ) -> _PrismaModelT:
        """Find a unique User record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            User filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The found User record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = await User.prisma().find_unique_or_raise(
            where={
                'id': 'biabhbdai',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
        include: Optional[types.UserInclude] = None,
        order: Optional[Union[types.UserOrderByInput, List[types.UserOrderByInput]]] = None,
        distinct: Optional[List[types.UserScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple User records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of User records returned
        skip
            Ignore the first N results
        where
            User filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned User model
        order
            Order the returned User records by any field
        distinct
            Filter User records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.User]
            The list of all User records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 User records
        users = await User.prisma().find_many(take=10)

        # find the first 5 User records ordered by the passwordHash field
        users = await User.prisma().find_many(
            take=5,
            order={
                'passwordHash': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
        include: Optional[types.UserInclude] = None,
        order: Optional[Union[types.UserOrderByInput, List[types.UserOrderByInput]]] = None,
        distinct: Optional[List[types.UserScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single User record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            User filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned User model
        order
            Order the returned User records by any field
        distinct
            Filter User records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.User
            The first User record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second User record ordered by the name field
        user = await User.prisma().find_first(
            skip=1,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
        include: Optional[types.UserInclude] = None,
        order: Optional[Union[types.UserOrderByInput, List[types.UserOrderByInput]]] = None,
        distinct: Optional[List[types.UserScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single User record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            User filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned User model
        order
            Order the returned User records by any field
        distinct
            Filter User records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.User
            The first User record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second User record ordered by the createdAt field
        user = await User.prisma().find_first_or_raise(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.UserUpdateInput,
        where: types.UserWhereUniqueInput,
        include: Optional[types.UserInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single User record.

        Parameters
        ----------
        data
            User record data specifying what to update
        where
            User filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The updated User record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await User.prisma().update(
            where={
                'id': 'idghgaicb',
            },
            data={
                # data to update the User record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.UserWhereUniqueInput,
        data: types.UserUpsertInput,
        include: Optional[types.UserInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            User filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned User model

        Returns
        -------
        prisma.models.User
            The created or updated User record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = await User.prisma().upsert(
            where={
                'id': 'fjfddhigg',
            },
            data={
                'create': {
                    'id': 'fjfddhigg',
                    'email': 'bfaiacjjfc',
                    'passwordHash': 'eigcfgbif',
                },
                'update': {
                    'email': 'bfaiacjjfc',
                    'passwordHash': 'eigcfgbif',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.UserUpdateManyMutationInput,
        where: types.UserWhereInput,
    ) -> int:
        """Update multiple User records

        Parameters
        ----------
        data
            User data to update the selected User records to
        where
            Filter to select the User records to update

        Returns
        -------
        int
            The total number of User records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all User records
        total = await User.prisma().update_many(
            data={
                'updatedAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
    ) -> int:
        """Count the number of User records present in the database

        Parameters
        ----------
        select
            Select the User fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            User filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.UserCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await User.prisma().count()

        # results: prisma.types.UserCountAggregateOutput
        results = await User.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.UserCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
    ) -> types.UserCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.UserCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWhereInput] = None,
        cursor: Optional[types.UserWhereUniqueInput] = None,
    ) -> Union[int, types.UserCountAggregateOutput]:
        """Count the number of User records present in the database

        Parameters
        ----------
        select
            Select the User fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            User filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.UserCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await User.prisma().count()

        # results: prisma.types.UserCountAggregateOutput
        results = await User.prisma().count(
            select={
                '_all': True,
                'email': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.UserCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.UserWhereInput] = None
    ) -> int:
        """Delete multiple User records.

        Parameters
        ----------
        where
            Optional User filter to find the records to be deleted

        Returns
        -------
        int
            The total number of User records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all User records
        total = await User.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.UserScalarFieldKeys'],
        *,
        where: Optional['types.UserWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.UserAvgAggregateInput'] = None,
        sum: Optional['types.UserSumAggregateInput'] = None,
        min: Optional['types.UserMinAggregateInput'] = None,
        max: Optional['types.UserMaxAggregateInput'] = None,
        having: Optional['types.UserScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.UserCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.UserScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.UserScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.UserGroupByOutput']:
        """Group User records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar User fields to group records by
        where
            User filter to select records
        take
            Limit the maximum number of User records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.UserGroupByOutput]
            A list of dictionaries representing the User record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group User records by passwordHash values
        # and count how many records are in each group
        results = await User.prisma().group_by(
            ['passwordHash'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ReadingPreferencesActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ReadingPreferences]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await ReadingPreferences.prisma().query_raw(
            'SELECT * FROM ReadingPreferences WHERE id = ?',
            'hjaecfifb',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ReadingPreferences
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await ReadingPreferences.prisma().query_first(
            'SELECT * FROM ReadingPreferences WHERE userId = ?',
            'cbbbjbfcii',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ReadingPreferencesCreateInput,
        include: Optional[types.ReadingPreferencesInclude] = None
    ) -> _PrismaModelT:
        """Create a new ReadingPreferences record.

        Parameters
        ----------
        data
            ReadingPreferences record data
        include
            Specifies which relations should be loaded on the returned ReadingPreferences model

        Returns
        -------
        prisma.models.ReadingPreferences
            The created ReadingPreferences record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ReadingPreferences record from just the required fields
        readingpreferences = await ReadingPreferences.prisma().create(
            data={
                # data to create a ReadingPreferences record
                'userId': 'bbejhfidcb',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ReadingPreferencesCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ReadingPreferences records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ReadingPreferences record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await ReadingPreferences.prisma().create_many(
            data=[
                {
                    # data to create a ReadingPreferences record
                    'userId': 'bgeecijdgg',
                },
                {
                    # data to create a ReadingPreferences record
                    'userId': 'bdiicjafbj',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ReadingPreferencesWhereUniqueInput,
        include: Optional[types.ReadingPreferencesInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ReadingPreferences record.

        Parameters
        ----------
        where
            ReadingPreferences filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ReadingPreferences model

        Returns
        -------
        prisma.models.ReadingPreferences
            The deleted ReadingPreferences record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        readingpreferences = await ReadingPreferences.prisma().delete(
            where={
                'id': 'bgehebiafc',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ReadingPreferencesWhereUniqueInput,
        include: Optional[types.ReadingPreferencesInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ReadingPreferences record.

        Parameters
        ----------
        where
            ReadingPreferences filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ReadingPreferences model

        Returns
        -------
        prisma.models.ReadingPreferences
            The found ReadingPreferences record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        readingpreferences = await ReadingPreferences.prisma().find_unique(
            where={
                'id': 'bghffegacj',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ReadingPreferencesWhereUniqueInput,
        include: Optional[types.ReadingPreferencesInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ReadingPreferences record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ReadingPreferences filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ReadingPreferences model

        Returns
        -------
        prisma.models.ReadingPreferences
            The found ReadingPreferences record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        readingpreferences = await ReadingPreferences.prisma().find_unique_or_raise(
            where={
                'id': 'bhghchehcc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReadingPreferencesWhereInput] = None,
        cursor: Optional[types.ReadingPreferencesWhereUniqueInput] = None,
        include: Optional[types.ReadingPreferencesInclude] = None,
        order: Optional[Union[types.ReadingPreferencesOrderByInput, List[types.ReadingPreferencesOrderByInput]]] = None,
        distinct: Optional[List[types.ReadingPreferencesScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ReadingPreferences records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ReadingPreferences records returned
        skip
            Ignore the first N results
        where
            ReadingPreferences filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ReadingPreferences model
        order
            Order the returned ReadingPreferences records by any field
        distinct
            Filter ReadingPreferences records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ReadingPreferences]
            The list of all ReadingPreferences records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ReadingPreferences records
        readingpreferences = await ReadingPreferences.prisma().find_many(take=10)

        # find the first 5 ReadingPreferences records ordered by the font field
        readingpreferences = await ReadingPreferences.prisma().find_many(
            take=5,
            order={
                'font': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ReadingPreferencesWhereInput] = None,
        cursor: Optional[types.ReadingPreferencesWhereUniqueInput] = None,
        include: Optional[types.ReadingPreferencesInclude] = None,
        order: Optional[Union[types.ReadingPreferencesOrderByInput, List[types.ReadingPreferencesOrderByInput]]] = None,
        distinct: Optional[List[types.ReadingPreferencesScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ReadingPreferences record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ReadingPreferences filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ReadingPreferences model
        order
            Order the returned ReadingPreferences records by any field
        distinct
            Filter ReadingPreferences records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ReadingPreferences
            The first ReadingPreferences record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ReadingPreferences record ordered by the fontSize field
        readingpreferences = await ReadingPreferences.prisma().find_first(
            skip=1,
            order={
                'fontSize': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ReadingPreferencesWhereInput] = None,
        cursor: Optional[types.ReadingPreferencesWhereUniqueInput] = None,
        include: Optional[types.ReadingPreferencesInclude] = None,
        order: Optional[Union[types.ReadingPreferencesOrderByInput, List[types.ReadingPreferencesOrderByInput]]] = None,
        distinct: Optional[List[types.ReadingPreferencesScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ReadingPreferences record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ReadingPreferences filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ReadingPreferences model
        order
            Order the returned ReadingPreferences records by any field
        distinct
            Filter ReadingPreferences records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ReadingPreferences
            The first ReadingPreferences record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ReadingPreferences record ordered by the letterSpacing field
        readingpreferences = await ReadingPreferences.prisma().find_first_or_raise(
            skip=1,
            order={
                'letterSpacing': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ReadingPreferencesUpdateInput,
        where: types.ReadingPreferencesWhereUniqueInput,
        include: Optional[types.ReadingPreferencesInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ReadingPreferences record.

        Parameters
        ----------
        data
            ReadingPreferences record data specifying what to update
        where
            ReadingPreferences filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ReadingPreferences model

        Returns
        -------
        prisma.models.ReadingPreferences
            The updated ReadingPreferences record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        readingpreferences = await ReadingPreferences.prisma().update(
            where={
                'id': 'dcgchcbbf',
            },
            data={
                # data to update the ReadingPreferences record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ReadingPreferencesWhereUniqueInput,
        data: types.ReadingPreferencesUpsertInput,
        include: Optional[types.ReadingPreferencesInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ReadingPreferences filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ReadingPreferences model

        Returns
        -------
        prisma.models.ReadingPreferences
            The created or updated ReadingPreferences record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        readingpreferences = await ReadingPreferences.prisma().upsert(
            where={
                'id': 'bdedcabahc',
            },
            data={
                'create': {
                    'id': 'bdedcabahc',
                    'userId': 'bdiicjafbj',
                },
                'update': {
                    'userId': 'bdiicjafbj',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ReadingPreferencesUpdateManyMutationInput,
        where: types.ReadingPreferencesWhereInput,
    ) -> int:
        """Update multiple ReadingPreferences records

        Parameters
        ----------
        data
            ReadingPreferences data to update the selected ReadingPreferences records to
        where
            Filter to select the ReadingPreferences records to update

        Returns
        -------
        int
            The total number of ReadingPreferences records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ReadingPreferences records
        total = await ReadingPreferences.prisma().update_many(
            data={
                'lineHeight': 675780521.74496
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReadingPreferencesWhereInput] = None,
        cursor: Optional[types.ReadingPreferencesWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ReadingPreferences records present in the database

        Parameters
        ----------
        select
            Select the ReadingPreferences fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ReadingPreferences filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ReadingPreferencesCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ReadingPreferences.prisma().count()

        # results: prisma.types.ReadingPreferencesCountAggregateOutput
        results = await ReadingPreferences.prisma().count(
            select={
                '_all': True,
                'theme': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ReadingPreferencesCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReadingPreferencesWhereInput] = None,
        cursor: Optional[types.ReadingPreferencesWhereUniqueInput] = None,
    ) -> types.ReadingPreferencesCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ReadingPreferencesCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReadingPreferencesWhereInput] = None,
        cursor: Optional[types.ReadingPreferencesWhereUniqueInput] = None,
    ) -> Union[int, types.ReadingPreferencesCountAggregateOutput]:
        """Count the number of ReadingPreferences records present in the database

        Parameters
        ----------
        select
            Select the ReadingPreferences fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ReadingPreferences filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ReadingPreferencesCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ReadingPreferences.prisma().count()

        # results: prisma.types.ReadingPreferencesCountAggregateOutput
        results = await ReadingPreferences.prisma().count(
            select={
                '_all': True,
                'focusMode': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ReadingPreferencesCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ReadingPreferencesWhereInput] = None
    ) -> int:
        """Delete multiple ReadingPreferences records.

        Parameters
        ----------
        where
            Optional ReadingPreferences filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ReadingPreferences records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ReadingPreferences records
        total = await ReadingPreferences.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ReadingPreferencesScalarFieldKeys'],
        *,
        where: Optional['types.ReadingPreferencesWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ReadingPreferencesAvgAggregateInput'] = None,
        sum: Optional['types.ReadingPreferencesSumAggregateInput'] = None,
        min: Optional['types.ReadingPreferencesMinAggregateInput'] = None,
        max: Optional['types.ReadingPreferencesMaxAggregateInput'] = None,
        having: Optional['types.ReadingPreferencesScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ReadingPreferencesCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ReadingPreferencesScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ReadingPreferencesScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ReadingPreferencesGroupByOutput']:
        """Group ReadingPreferences records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ReadingPreferences fields to group records by
        where
            ReadingPreferences filter to select records
        take
            Limit the maximum number of ReadingPreferences records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ReadingPreferencesGroupByOutput]
            A list of dictionaries representing the ReadingPreferences record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ReadingPreferences records by speechSpeed values
        # and count how many records are in each group
        results = await ReadingPreferences.prisma().group_by(
            ['speechSpeed'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class LearningProgressActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.LearningProgress]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await LearningProgress.prisma().query_raw(
            'SELECT * FROM LearningProgress WHERE id = ?',
            'bjgjgibgbf',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.LearningProgress
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await LearningProgress.prisma().query_first(
            'SELECT * FROM LearningProgress WHERE userId = ?',
            'bbbgbhfjge',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.LearningProgressCreateInput,
        include: Optional[types.LearningProgressInclude] = None
    ) -> _PrismaModelT:
        """Create a new LearningProgress record.

        Parameters
        ----------
        data
            LearningProgress record data
        include
            Specifies which relations should be loaded on the returned LearningProgress model

        Returns
        -------
        prisma.models.LearningProgress
            The created LearningProgress record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a LearningProgress record from just the required fields
        learningprogress = await LearningProgress.prisma().create(
            data={
                # data to create a LearningProgress record
                'userId': 'igbehcbab',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.LearningProgressCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple LearningProgress records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of LearningProgress record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await LearningProgress.prisma().create_many(
            data=[
                {
                    # data to create a LearningProgress record
                    'userId': 'bdadaadhag',
                },
                {
                    # data to create a LearningProgress record
                    'userId': 'bgiggdidbf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.LearningProgressWhereUniqueInput,
        include: Optional[types.LearningProgressInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single LearningProgress record.

        Parameters
        ----------
        where
            LearningProgress filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned LearningProgress model

        Returns
        -------
        prisma.models.LearningProgress
            The deleted LearningProgress record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        learningprogress = await LearningProgress.prisma().delete(
            where={
                'id': 'caaaedabfc',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.LearningProgressWhereUniqueInput,
        include: Optional[types.LearningProgressInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique LearningProgress record.

        Parameters
        ----------
        where
            LearningProgress filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned LearningProgress model

        Returns
        -------
        prisma.models.LearningProgress
            The found LearningProgress record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        learningprogress = await LearningProgress.prisma().find_unique(
            where={
                'id': 'bigibebcib',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.LearningProgressWhereUniqueInput,
        include: Optional[types.LearningProgressInclude] = None
    ) -> _PrismaModelT:
        """Find a unique LearningProgress record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            LearningProgress filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned LearningProgress model

        Returns
        -------
        prisma.models.LearningProgress
            The found LearningProgress record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        learningprogress = await LearningProgress.prisma().find_unique_or_raise(
            where={
                'id': 'bigaiehgcc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LearningProgressWhereInput] = None,
        cursor: Optional[types.LearningProgressWhereUniqueInput] = None,
        include: Optional[types.LearningProgressInclude] = None,
        order: Optional[Union[types.LearningProgressOrderByInput, List[types.LearningProgressOrderByInput]]] = None,
        distinct: Optional[List[types.LearningProgressScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple LearningProgress records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of LearningProgress records returned
        skip
            Ignore the first N results
        where
            LearningProgress filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LearningProgress model
        order
            Order the returned LearningProgress records by any field
        distinct
            Filter LearningProgress records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.LearningProgress]
            The list of all LearningProgress records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 LearningProgress records
        learningprogress = await LearningProgress.prisma().find_many(take=10)

        # find the first 5 LearningProgress records ordered by the readingSpeed field
        learningprogress = await LearningProgress.prisma().find_many(
            take=5,
            order={
                'readingSpeed': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.LearningProgressWhereInput] = None,
        cursor: Optional[types.LearningProgressWhereUniqueInput] = None,
        include: Optional[types.LearningProgressInclude] = None,
        order: Optional[Union[types.LearningProgressOrderByInput, List[types.LearningProgressOrderByInput]]] = None,
        distinct: Optional[List[types.LearningProgressScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single LearningProgress record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            LearningProgress filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LearningProgress model
        order
            Order the returned LearningProgress records by any field
        distinct
            Filter LearningProgress records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.LearningProgress
            The first LearningProgress record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second LearningProgress record ordered by the spellingAccuracy field
        learningprogress = await LearningProgress.prisma().find_first(
            skip=1,
            order={
                'spellingAccuracy': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.LearningProgressWhereInput] = None,
        cursor: Optional[types.LearningProgressWhereUniqueInput] = None,
        include: Optional[types.LearningProgressInclude] = None,
        order: Optional[Union[types.LearningProgressOrderByInput, List[types.LearningProgressOrderByInput]]] = None,
        distinct: Optional[List[types.LearningProgressScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single LearningProgress record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            LearningProgress filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LearningProgress model
        order
            Order the returned LearningProgress records by any field
        distinct
            Filter LearningProgress records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.LearningProgress
            The first LearningProgress record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second LearningProgress record ordered by the gamesCompleted field
        learningprogress = await LearningProgress.prisma().find_first_or_raise(
            skip=1,
            order={
                'gamesCompleted': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.LearningProgressUpdateInput,
        where: types.LearningProgressWhereUniqueInput,
        include: Optional[types.LearningProgressInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single LearningProgress record.

        Parameters
        ----------
        data
            LearningProgress record data specifying what to update
        where
            LearningProgress filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned LearningProgress model

        Returns
        -------
        prisma.models.LearningProgress
            The updated LearningProgress record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        learningprogress = await LearningProgress.prisma().update(
            where={
                'id': 'beeifcbebf',
            },
            data={
                # data to update the LearningProgress record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.LearningProgressWhereUniqueInput,
        data: types.LearningProgressUpsertInput,
        include: Optional[types.LearningProgressInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            LearningProgress filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned LearningProgress model

        Returns
        -------
        prisma.models.LearningProgress
            The created or updated LearningProgress record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        learningprogress = await LearningProgress.prisma().upsert(
            where={
                'id': 'bgcigfahea',
            },
            data={
                'create': {
                    'id': 'bgcigfahea',
                    'userId': 'bgiggdidbf',
                },
                'update': {
                    'userId': 'bgiggdidbf',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.LearningProgressUpdateManyMutationInput,
        where: types.LearningProgressWhereInput,
    ) -> int:
        """Update multiple LearningProgress records

        Parameters
        ----------
        data
            LearningProgress data to update the selected LearningProgress records to
        where
            Filter to select the LearningProgress records to update

        Returns
        -------
        int
            The total number of LearningProgress records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all LearningProgress records
        total = await LearningProgress.prisma().update_many(
            data={
                'totalTimeMinutes': 1249606685
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LearningProgressWhereInput] = None,
        cursor: Optional[types.LearningProgressWhereUniqueInput] = None,
    ) -> int:
        """Count the number of LearningProgress records present in the database

        Parameters
        ----------
        select
            Select the LearningProgress fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            LearningProgress filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.LearningProgressCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await LearningProgress.prisma().count()

        # results: prisma.types.LearningProgressCountAggregateOutput
        results = await LearningProgress.prisma().count(
            select={
                '_all': True,
                'currentStreak': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.LearningProgressCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LearningProgressWhereInput] = None,
        cursor: Optional[types.LearningProgressWhereUniqueInput] = None,
    ) -> types.LearningProgressCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.LearningProgressCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LearningProgressWhereInput] = None,
        cursor: Optional[types.LearningProgressWhereUniqueInput] = None,
    ) -> Union[int, types.LearningProgressCountAggregateOutput]:
        """Count the number of LearningProgress records present in the database

        Parameters
        ----------
        select
            Select the LearningProgress fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            LearningProgress filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.LearningProgressCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await LearningProgress.prisma().count()

        # results: prisma.types.LearningProgressCountAggregateOutput
        results = await LearningProgress.prisma().count(
            select={
                '_all': True,
                'longestStreak': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.LearningProgressCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.LearningProgressWhereInput] = None
    ) -> int:
        """Delete multiple LearningProgress records.

        Parameters
        ----------
        where
            Optional LearningProgress filter to find the records to be deleted

        Returns
        -------
        int
            The total number of LearningProgress records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all LearningProgress records
        total = await LearningProgress.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.LearningProgressScalarFieldKeys'],
        *,
        where: Optional['types.LearningProgressWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.LearningProgressAvgAggregateInput'] = None,
        sum: Optional['types.LearningProgressSumAggregateInput'] = None,
        min: Optional['types.LearningProgressMinAggregateInput'] = None,
        max: Optional['types.LearningProgressMaxAggregateInput'] = None,
        having: Optional['types.LearningProgressScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.LearningProgressCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.LearningProgressScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.LearningProgressScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.LearningProgressGroupByOutput']:
        """Group LearningProgress records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar LearningProgress fields to group records by
        where
            LearningProgress filter to select records
        take
            Limit the maximum number of LearningProgress records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.LearningProgressGroupByOutput]
            A list of dictionaries representing the LearningProgress record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group LearningProgress records by lastActivityDate values
        # and count how many records are in each group
        results = await LearningProgress.prisma().group_by(
            ['lastActivityDate'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class GameScoreActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.GameScore]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await GameScore.prisma().query_raw(
            'SELECT * FROM GameScore WHERE id = ?',
            'idfjadbcc',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.GameScore
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await GameScore.prisma().query_first(
            'SELECT * FROM GameScore WHERE userId = ?',
            'hgdhbjhhj',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.GameScoreCreateInput,
        include: Optional[types.GameScoreInclude] = None
    ) -> _PrismaModelT:
        """Create a new GameScore record.

        Parameters
        ----------
        data
            GameScore record data
        include
            Specifies which relations should be loaded on the returned GameScore model

        Returns
        -------
        prisma.models.GameScore
            The created GameScore record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a GameScore record from just the required fields
        gamescore = await GameScore.prisma().create(
            data={
                # data to create a GameScore record
                'userId': 'ecjjjfbae',
                'gameType': 'bhhfibbigf',
                'score': 893145566,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.GameScoreCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple GameScore records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of GameScore record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await GameScore.prisma().create_many(
            data=[
                {
                    # data to create a GameScore record
                    'userId': 'jjfeafhfj',
                    'gameType': 'cbachdgfce',
                    'score': 271520213,
                },
                {
                    # data to create a GameScore record
                    'userId': 'efggddide',
                    'gameType': 'caficfigfb',
                    'score': 1583689592,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.GameScoreWhereUniqueInput,
        include: Optional[types.GameScoreInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single GameScore record.

        Parameters
        ----------
        where
            GameScore filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned GameScore model

        Returns
        -------
        prisma.models.GameScore
            The deleted GameScore record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        gamescore = await GameScore.prisma().delete(
            where={
                'id': 'ihieecagf',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.GameScoreWhereUniqueInput,
        include: Optional[types.GameScoreInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique GameScore record.

        Parameters
        ----------
        where
            GameScore filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned GameScore model

        Returns
        -------
        prisma.models.GameScore
            The found GameScore record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        gamescore = await GameScore.prisma().find_unique(
            where={
                'id': 'bghfciaafe',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.GameScoreWhereUniqueInput,
        include: Optional[types.GameScoreInclude] = None
    ) -> _PrismaModelT:
        """Find a unique GameScore record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            GameScore filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned GameScore model

        Returns
        -------
        prisma.models.GameScore
            The found GameScore record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        gamescore = await GameScore.prisma().find_unique_or_raise(
            where={
                'id': 'bgchfhgceh',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GameScoreWhereInput] = None,
        cursor: Optional[types.GameScoreWhereUniqueInput] = None,
        include: Optional[types.GameScoreInclude] = None,
        order: Optional[Union[types.GameScoreOrderByInput, List[types.GameScoreOrderByInput]]] = None,
        distinct: Optional[List[types.GameScoreScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple GameScore records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of GameScore records returned
        skip
            Ignore the first N results
        where
            GameScore filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned GameScore model
        order
            Order the returned GameScore records by any field
        distinct
            Filter GameScore records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.GameScore]
            The list of all GameScore records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 GameScore records
        gamescores = await GameScore.prisma().find_many(take=10)

        # find the first 5 GameScore records ordered by the gameType field
        gamescores = await GameScore.prisma().find_many(
            take=5,
            order={
                'gameType': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.GameScoreWhereInput] = None,
        cursor: Optional[types.GameScoreWhereUniqueInput] = None,
        include: Optional[types.GameScoreInclude] = None,
        order: Optional[Union[types.GameScoreOrderByInput, List[types.GameScoreOrderByInput]]] = None,
        distinct: Optional[List[types.GameScoreScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single GameScore record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            GameScore filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned GameScore model
        order
            Order the returned GameScore records by any field
        distinct
            Filter GameScore records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.GameScore
            The first GameScore record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second GameScore record ordered by the score field
        gamescore = await GameScore.prisma().find_first(
            skip=1,
            order={
                'score': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.GameScoreWhereInput] = None,
        cursor: Optional[types.GameScoreWhereUniqueInput] = None,
        include: Optional[types.GameScoreInclude] = None,
        order: Optional[Union[types.GameScoreOrderByInput, List[types.GameScoreOrderByInput]]] = None,
        distinct: Optional[List[types.GameScoreScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single GameScore record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            GameScore filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned GameScore model
        order
            Order the returned GameScore records by any field
        distinct
            Filter GameScore records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.GameScore
            The first GameScore record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second GameScore record ordered by the accuracy field
        gamescore = await GameScore.prisma().find_first_or_raise(
            skip=1,
            order={
                'accuracy': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.GameScoreUpdateInput,
        where: types.GameScoreWhereUniqueInput,
        include: Optional[types.GameScoreInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single GameScore record.

        Parameters
        ----------
        data
            GameScore record data specifying what to update
        where
            GameScore filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned GameScore model

        Returns
        -------
        prisma.models.GameScore
            The updated GameScore record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        gamescore = await GameScore.prisma().update(
            where={
                'id': 'cafeiaccbc',
            },
            data={
                # data to update the GameScore record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.GameScoreWhereUniqueInput,
        data: types.GameScoreUpsertInput,
        include: Optional[types.GameScoreInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            GameScore filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned GameScore model

        Returns
        -------
        prisma.models.GameScore
            The created or updated GameScore record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        gamescore = await GameScore.prisma().upsert(
            where={
                'id': 'gaddfhfh',
            },
            data={
                'create': {
                    'id': 'gaddfhfh',
                    'userId': 'efggddide',
                    'gameType': 'caficfigfb',
                    'score': 1583689592,
                },
                'update': {
                    'userId': 'efggddide',
                    'gameType': 'caficfigfb',
                    'score': 1583689592,
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.GameScoreUpdateManyMutationInput,
        where: types.GameScoreWhereInput,
    ) -> int:
        """Update multiple GameScore records

        Parameters
        ----------
        data
            GameScore data to update the selected GameScore records to
        where
            Filter to select the GameScore records to update

        Returns
        -------
        int
            The total number of GameScore records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all GameScore records
        total = await GameScore.prisma().update_many(
            data={
                'timeTaken': 684462146
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GameScoreWhereInput] = None,
        cursor: Optional[types.GameScoreWhereUniqueInput] = None,
    ) -> int:
        """Count the number of GameScore records present in the database

        Parameters
        ----------
        select
            Select the GameScore fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            GameScore filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.GameScoreCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await GameScore.prisma().count()

        # results: prisma.types.GameScoreCountAggregateOutput
        results = await GameScore.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.GameScoreCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GameScoreWhereInput] = None,
        cursor: Optional[types.GameScoreWhereUniqueInput] = None,
    ) -> types.GameScoreCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.GameScoreCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.GameScoreWhereInput] = None,
        cursor: Optional[types.GameScoreWhereUniqueInput] = None,
    ) -> Union[int, types.GameScoreCountAggregateOutput]:
        """Count the number of GameScore records present in the database

        Parameters
        ----------
        select
            Select the GameScore fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            GameScore filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.GameScoreCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await GameScore.prisma().count()

        # results: prisma.types.GameScoreCountAggregateOutput
        results = await GameScore.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.GameScoreCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.GameScoreWhereInput] = None
    ) -> int:
        """Delete multiple GameScore records.

        Parameters
        ----------
        where
            Optional GameScore filter to find the records to be deleted

        Returns
        -------
        int
            The total number of GameScore records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all GameScore records
        total = await GameScore.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.GameScoreScalarFieldKeys'],
        *,
        where: Optional['types.GameScoreWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.GameScoreAvgAggregateInput'] = None,
        sum: Optional['types.GameScoreSumAggregateInput'] = None,
        min: Optional['types.GameScoreMinAggregateInput'] = None,
        max: Optional['types.GameScoreMaxAggregateInput'] = None,
        having: Optional['types.GameScoreScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.GameScoreCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.GameScoreScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.GameScoreScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.GameScoreGroupByOutput']:
        """Group GameScore records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar GameScore fields to group records by
        where
            GameScore filter to select records
        take
            Limit the maximum number of GameScore records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.GameScoreGroupByOutput]
            A list of dictionaries representing the GameScore record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group GameScore records by userId values
        # and count how many records are in each group
        results = await GameScore.prisma().group_by(
            ['userId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ActivityActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Activity]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Activity.prisma().query_raw(
            'SELECT * FROM Activity WHERE id = ?',
            'bgcffadich',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Activity
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Activity.prisma().query_first(
            'SELECT * FROM Activity WHERE userId = ?',
            'fcbichhci',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ActivityCreateInput,
        include: Optional[types.ActivityInclude] = None
    ) -> _PrismaModelT:
        """Create a new Activity record.

        Parameters
        ----------
        data
            Activity record data
        include
            Specifies which relations should be loaded on the returned Activity model

        Returns
        -------
        prisma.models.Activity
            The created Activity record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Activity record from just the required fields
        activity = await Activity.prisma().create(
            data={
                # data to create a Activity record
                'userId': 'bcggadccgf',
                'activityType': 'jdcfdcgc',
                'description': 'cafdaehjid',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ActivityCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Activity records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Activity record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Activity.prisma().create_many(
            data=[
                {
                    # data to create a Activity record
                    'userId': 'gifdddbia',
                    'activityType': 'bchehecef',
                    'description': 'jeijcbhfe',
                },
                {
                    # data to create a Activity record
                    'userId': 'bjgejjabff',
                    'activityType': 'bcciijbibg',
                    'description': 'cffcachfd',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ActivityWhereUniqueInput,
        include: Optional[types.ActivityInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Activity record.

        Parameters
        ----------
        where
            Activity filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Activity model

        Returns
        -------
        prisma.models.Activity
            The deleted Activity record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        activity = await Activity.prisma().delete(
            where={
                'id': 'bccdfhdigc',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ActivityWhereUniqueInput,
        include: Optional[types.ActivityInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Activity record.

        Parameters
        ----------
        where
            Activity filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Activity model

        Returns
        -------
        prisma.models.Activity
            The found Activity record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        activity = await Activity.prisma().find_unique(
            where={
                'id': 'febcgjbfj',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ActivityWhereUniqueInput,
        include: Optional[types.ActivityInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Activity record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Activity filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Activity model

        Returns
        -------
        prisma.models.Activity
            The found Activity record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        activity = await Activity.prisma().find_unique_or_raise(
            where={
                'id': 'bageiegghg',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ActivityWhereInput] = None,
        cursor: Optional[types.ActivityWhereUniqueInput] = None,
        include: Optional[types.ActivityInclude] = None,
        order: Optional[Union[types.ActivityOrderByInput, List[types.ActivityOrderByInput]]] = None,
        distinct: Optional[List[types.ActivityScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Activity records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Activity records returned
        skip
            Ignore the first N results
        where
            Activity filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Activity model
        order
            Order the returned Activity records by any field
        distinct
            Filter Activity records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Activity]
            The list of all Activity records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Activity records
        activitys = await Activity.prisma().find_many(take=10)

        # find the first 5 Activity records ordered by the activityType field
        activitys = await Activity.prisma().find_many(
            take=5,
            order={
                'activityType': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ActivityWhereInput] = None,
        cursor: Optional[types.ActivityWhereUniqueInput] = None,
        include: Optional[types.ActivityInclude] = None,
        order: Optional[Union[types.ActivityOrderByInput, List[types.ActivityOrderByInput]]] = None,
        distinct: Optional[List[types.ActivityScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Activity record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Activity filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Activity model
        order
            Order the returned Activity records by any field
        distinct
            Filter Activity records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Activity
            The first Activity record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Activity record ordered by the description field
        activity = await Activity.prisma().find_first(
            skip=1,
            order={
                'description': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ActivityWhereInput] = None,
        cursor: Optional[types.ActivityWhereUniqueInput] = None,
        include: Optional[types.ActivityInclude] = None,
        order: Optional[Union[types.ActivityOrderByInput, List[types.ActivityOrderByInput]]] = None,
        distinct: Optional[List[types.ActivityScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Activity record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Activity filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Activity model
        order
            Order the returned Activity records by any field
        distinct
            Filter Activity records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Activity
            The first Activity record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Activity record ordered by the metadata field
        activity = await Activity.prisma().find_first_or_raise(
            skip=1,
            order={
                'metadata': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ActivityUpdateInput,
        where: types.ActivityWhereUniqueInput,
        include: Optional[types.ActivityInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Activity record.

        Parameters
        ----------
        data
            Activity record data specifying what to update
        where
            Activity filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Activity model

        Returns
        -------
        prisma.models.Activity
            The updated Activity record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        activity = await Activity.prisma().update(
            where={
                'id': 'faidicegb',
            },
            data={
                # data to update the Activity record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ActivityWhereUniqueInput,
        data: types.ActivityUpsertInput,
        include: Optional[types.ActivityInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Activity filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Activity model

        Returns
        -------
        prisma.models.Activity
            The created or updated Activity record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        activity = await Activity.prisma().upsert(
            where={
                'id': 'bacecgfhbe',
            },
            data={
                'create': {
                    'id': 'bacecgfhbe',
                    'userId': 'bjgejjabff',
                    'activityType': 'bcciijbibg',
                    'description': 'cffcachfd',
                },
                'update': {
                    'userId': 'bjgejjabff',
                    'activityType': 'bcciijbibg',
                    'description': 'cffcachfd',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ActivityUpdateManyMutationInput,
        where: types.ActivityWhereInput,
    ) -> int:
        """Update multiple Activity records

        Parameters
        ----------
        data
            Activity data to update the selected Activity records to
        where
            Filter to select the Activity records to update

        Returns
        -------
        int
            The total number of Activity records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Activity records
        total = await Activity.prisma().update_many(
            data={
                'createdAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ActivityWhereInput] = None,
        cursor: Optional[types.ActivityWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Activity records present in the database

        Parameters
        ----------
        select
            Select the Activity fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Activity filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ActivityCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Activity.prisma().count()

        # results: prisma.types.ActivityCountAggregateOutput
        results = await Activity.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ActivityCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ActivityWhereInput] = None,
        cursor: Optional[types.ActivityWhereUniqueInput] = None,
    ) -> types.ActivityCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ActivityCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ActivityWhereInput] = None,
        cursor: Optional[types.ActivityWhereUniqueInput] = None,
    ) -> Union[int, types.ActivityCountAggregateOutput]:
        """Count the number of Activity records present in the database

        Parameters
        ----------
        select
            Select the Activity fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Activity filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ActivityCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Activity.prisma().count()

        # results: prisma.types.ActivityCountAggregateOutput
        results = await Activity.prisma().count(
            select={
                '_all': True,
                'userId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ActivityCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ActivityWhereInput] = None
    ) -> int:
        """Delete multiple Activity records.

        Parameters
        ----------
        where
            Optional Activity filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Activity records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Activity records
        total = await Activity.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ActivityScalarFieldKeys'],
        *,
        where: Optional['types.ActivityWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ActivityAvgAggregateInput'] = None,
        sum: Optional['types.ActivitySumAggregateInput'] = None,
        min: Optional['types.ActivityMinAggregateInput'] = None,
        max: Optional['types.ActivityMaxAggregateInput'] = None,
        having: Optional['types.ActivityScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ActivityCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ActivityScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ActivityScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ActivityGroupByOutput']:
        """Group Activity records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Activity fields to group records by
        where
            Activity filter to select records
        take
            Limit the maximum number of Activity records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ActivityGroupByOutput]
            A list of dictionaries representing the Activity record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Activity records by activityType values
        # and count how many records are in each group
        results = await Activity.prisma().group_by(
            ['activityType'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ReadingSessionActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ReadingSession]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await ReadingSession.prisma().query_raw(
            'SELECT * FROM ReadingSession WHERE id = ?',
            'ihcahiead',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ReadingSession
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await ReadingSession.prisma().query_first(
            'SELECT * FROM ReadingSession WHERE userId = ?',
            'biheheiajg',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ReadingSessionCreateInput,
        include: Optional[types.ReadingSessionInclude] = None
    ) -> _PrismaModelT:
        """Create a new ReadingSession record.

        Parameters
        ----------
        data
            ReadingSession record data
        include
            Specifies which relations should be loaded on the returned ReadingSession model

        Returns
        -------
        prisma.models.ReadingSession
            The created ReadingSession record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ReadingSession record from just the required fields
        readingsession = await ReadingSession.prisma().create(
            data={
                # data to create a ReadingSession record
                'userId': 'jbgijghgb',
                'textId': 'hgjcghfbi',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ReadingSessionCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ReadingSession records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ReadingSession record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await ReadingSession.prisma().create_many(
            data=[
                {
                    # data to create a ReadingSession record
                    'userId': 'icadbcehj',
                    'textId': 'jchciaee',
                },
                {
                    # data to create a ReadingSession record
                    'userId': 'deeificjd',
                    'textId': 'bbcbhebbda',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ReadingSessionWhereUniqueInput,
        include: Optional[types.ReadingSessionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ReadingSession record.

        Parameters
        ----------
        where
            ReadingSession filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ReadingSession model

        Returns
        -------
        prisma.models.ReadingSession
            The deleted ReadingSession record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        readingsession = await ReadingSession.prisma().delete(
            where={
                'id': 'bejfijgcfb',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ReadingSessionWhereUniqueInput,
        include: Optional[types.ReadingSessionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ReadingSession record.

        Parameters
        ----------
        where
            ReadingSession filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ReadingSession model

        Returns
        -------
        prisma.models.ReadingSession
            The found ReadingSession record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        readingsession = await ReadingSession.prisma().find_unique(
            where={
                'id': 'caifcbgii',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ReadingSessionWhereUniqueInput,
        include: Optional[types.ReadingSessionInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ReadingSession record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ReadingSession filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ReadingSession model

        Returns
        -------
        prisma.models.ReadingSession
            The found ReadingSession record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        readingsession = await ReadingSession.prisma().find_unique_or_raise(
            where={
                'id': 'igaibbfgj',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReadingSessionWhereInput] = None,
        cursor: Optional[types.ReadingSessionWhereUniqueInput] = None,
        include: Optional[types.ReadingSessionInclude] = None,
        order: Optional[Union[types.ReadingSessionOrderByInput, List[types.ReadingSessionOrderByInput]]] = None,
        distinct: Optional[List[types.ReadingSessionScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ReadingSession records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ReadingSession records returned
        skip
            Ignore the first N results
        where
            ReadingSession filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ReadingSession model
        order
            Order the returned ReadingSession records by any field
        distinct
            Filter ReadingSession records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ReadingSession]
            The list of all ReadingSession records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ReadingSession records
        readingsessions = await ReadingSession.prisma().find_many(take=10)

        # find the first 5 ReadingSession records ordered by the textId field
        readingsessions = await ReadingSession.prisma().find_many(
            take=5,
            order={
                'textId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ReadingSessionWhereInput] = None,
        cursor: Optional[types.ReadingSessionWhereUniqueInput] = None,
        include: Optional[types.ReadingSessionInclude] = None,
        order: Optional[Union[types.ReadingSessionOrderByInput, List[types.ReadingSessionOrderByInput]]] = None,
        distinct: Optional[List[types.ReadingSessionScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ReadingSession record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ReadingSession filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ReadingSession model
        order
            Order the returned ReadingSession records by any field
        distinct
            Filter ReadingSession records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ReadingSession
            The first ReadingSession record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ReadingSession record ordered by the textContent field
        readingsession = await ReadingSession.prisma().find_first(
            skip=1,
            order={
                'textContent': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ReadingSessionWhereInput] = None,
        cursor: Optional[types.ReadingSessionWhereUniqueInput] = None,
        include: Optional[types.ReadingSessionInclude] = None,
        order: Optional[Union[types.ReadingSessionOrderByInput, List[types.ReadingSessionOrderByInput]]] = None,
        distinct: Optional[List[types.ReadingSessionScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ReadingSession record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ReadingSession filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ReadingSession model
        order
            Order the returned ReadingSession records by any field
        distinct
            Filter ReadingSession records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ReadingSession
            The first ReadingSession record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ReadingSession record ordered by the startedAt field
        readingsession = await ReadingSession.prisma().find_first_or_raise(
            skip=1,
            order={
                'startedAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ReadingSessionUpdateInput,
        where: types.ReadingSessionWhereUniqueInput,
        include: Optional[types.ReadingSessionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ReadingSession record.

        Parameters
        ----------
        data
            ReadingSession record data specifying what to update
        where
            ReadingSession filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ReadingSession model

        Returns
        -------
        prisma.models.ReadingSession
            The updated ReadingSession record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        readingsession = await ReadingSession.prisma().update(
            where={
                'id': 'bggajdcbbi',
            },
            data={
                # data to update the ReadingSession record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ReadingSessionWhereUniqueInput,
        data: types.ReadingSessionUpsertInput,
        include: Optional[types.ReadingSessionInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ReadingSession filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ReadingSession model

        Returns
        -------
        prisma.models.ReadingSession
            The created or updated ReadingSession record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        readingsession = await ReadingSession.prisma().upsert(
            where={
                'id': 'fcfhgbjed',
            },
            data={
                'create': {
                    'id': 'fcfhgbjed',
                    'userId': 'deeificjd',
                    'textId': 'bbcbhebbda',
                },
                'update': {
                    'userId': 'deeificjd',
                    'textId': 'bbcbhebbda',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ReadingSessionUpdateManyMutationInput,
        where: types.ReadingSessionWhereInput,
    ) -> int:
        """Update multiple ReadingSession records

        Parameters
        ----------
        data
            ReadingSession data to update the selected ReadingSession records to
        where
            Filter to select the ReadingSession records to update

        Returns
        -------
        int
            The total number of ReadingSession records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ReadingSession records
        total = await ReadingSession.prisma().update_many(
            data={
                'completedAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReadingSessionWhereInput] = None,
        cursor: Optional[types.ReadingSessionWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ReadingSession records present in the database

        Parameters
        ----------
        select
            Select the ReadingSession fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ReadingSession filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ReadingSessionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ReadingSession.prisma().count()

        # results: prisma.types.ReadingSessionCountAggregateOutput
        results = await ReadingSession.prisma().count(
            select={
                '_all': True,
                'wpm': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ReadingSessionCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReadingSessionWhereInput] = None,
        cursor: Optional[types.ReadingSessionWhereUniqueInput] = None,
    ) -> types.ReadingSessionCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ReadingSessionCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReadingSessionWhereInput] = None,
        cursor: Optional[types.ReadingSessionWhereUniqueInput] = None,
    ) -> Union[int, types.ReadingSessionCountAggregateOutput]:
        """Count the number of ReadingSession records present in the database

        Parameters
        ----------
        select
            Select the ReadingSession fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ReadingSession filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ReadingSessionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ReadingSession.prisma().count()

        # results: prisma.types.ReadingSessionCountAggregateOutput
        results = await ReadingSession.prisma().count(
            select={
                '_all': True,
                'difficultyScore': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ReadingSessionCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ReadingSessionWhereInput] = None
    ) -> int:
        """Delete multiple ReadingSession records.

        Parameters
        ----------
        where
            Optional ReadingSession filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ReadingSession records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ReadingSession records
        total = await ReadingSession.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ReadingSessionScalarFieldKeys'],
        *,
        where: Optional['types.ReadingSessionWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ReadingSessionAvgAggregateInput'] = None,
        sum: Optional['types.ReadingSessionSumAggregateInput'] = None,
        min: Optional['types.ReadingSessionMinAggregateInput'] = None,
        max: Optional['types.ReadingSessionMaxAggregateInput'] = None,
        having: Optional['types.ReadingSessionScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ReadingSessionCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ReadingSessionScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ReadingSessionScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ReadingSessionGroupByOutput']:
        """Group ReadingSession records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ReadingSession fields to group records by
        where
            ReadingSession filter to select records
        take
            Limit the maximum number of ReadingSession records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ReadingSessionGroupByOutput]
            A list of dictionaries representing the ReadingSession record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ReadingSession records by createdAt values
        # and count how many records are in each group
        results = await ReadingSession.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ReadingEventActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ReadingEvent]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await ReadingEvent.prisma().query_raw(
            'SELECT * FROM ReadingEvent WHERE id = ?',
            'hdgcajhjg',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ReadingEvent
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await ReadingEvent.prisma().query_first(
            'SELECT * FROM ReadingEvent WHERE sessionId = ?',
            'ejdjahicb',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ReadingEventCreateInput,
        include: Optional[types.ReadingEventInclude] = None
    ) -> _PrismaModelT:
        """Create a new ReadingEvent record.

        Parameters
        ----------
        data
            ReadingEvent record data
        include
            Specifies which relations should be loaded on the returned ReadingEvent model

        Returns
        -------
        prisma.models.ReadingEvent
            The created ReadingEvent record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ReadingEvent record from just the required fields
        readingevent = await ReadingEvent.prisma().create(
            data={
                # data to create a ReadingEvent record
                'sessionId': 'gdjgigfgc',
                'eventType': 'gfeaahdeh',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ReadingEventCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ReadingEvent records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ReadingEvent record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await ReadingEvent.prisma().create_many(
            data=[
                {
                    # data to create a ReadingEvent record
                    'sessionId': 'bjafcgbffc',
                    'eventType': 'hihegjif',
                },
                {
                    # data to create a ReadingEvent record
                    'sessionId': 'bdjidcidac',
                    'eventType': 'ifgaaagff',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ReadingEventWhereUniqueInput,
        include: Optional[types.ReadingEventInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ReadingEvent record.

        Parameters
        ----------
        where
            ReadingEvent filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ReadingEvent model

        Returns
        -------
        prisma.models.ReadingEvent
            The deleted ReadingEvent record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        readingevent = await ReadingEvent.prisma().delete(
            where={
                'id': 'befcddgjce',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ReadingEventWhereUniqueInput,
        include: Optional[types.ReadingEventInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ReadingEvent record.

        Parameters
        ----------
        where
            ReadingEvent filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ReadingEvent model

        Returns
        -------
        prisma.models.ReadingEvent
            The found ReadingEvent record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        readingevent = await ReadingEvent.prisma().find_unique(
            where={
                'id': 'bfhdbjjgfd',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ReadingEventWhereUniqueInput,
        include: Optional[types.ReadingEventInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ReadingEvent record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ReadingEvent filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ReadingEvent model

        Returns
        -------
        prisma.models.ReadingEvent
            The found ReadingEvent record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        readingevent = await ReadingEvent.prisma().find_unique_or_raise(
            where={
                'id': 'cabdjadaji',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReadingEventWhereInput] = None,
        cursor: Optional[types.ReadingEventWhereUniqueInput] = None,
        include: Optional[types.ReadingEventInclude] = None,
        order: Optional[Union[types.ReadingEventOrderByInput, List[types.ReadingEventOrderByInput]]] = None,
        distinct: Optional[List[types.ReadingEventScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ReadingEvent records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ReadingEvent records returned
        skip
            Ignore the first N results
        where
            ReadingEvent filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ReadingEvent model
        order
            Order the returned ReadingEvent records by any field
        distinct
            Filter ReadingEvent records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ReadingEvent]
            The list of all ReadingEvent records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ReadingEvent records
        readingevents = await ReadingEvent.prisma().find_many(take=10)

        # find the first 5 ReadingEvent records ordered by the eventType field
        readingevents = await ReadingEvent.prisma().find_many(
            take=5,
            order={
                'eventType': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ReadingEventWhereInput] = None,
        cursor: Optional[types.ReadingEventWhereUniqueInput] = None,
        include: Optional[types.ReadingEventInclude] = None,
        order: Optional[Union[types.ReadingEventOrderByInput, List[types.ReadingEventOrderByInput]]] = None,
        distinct: Optional[List[types.ReadingEventScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ReadingEvent record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ReadingEvent filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ReadingEvent model
        order
            Order the returned ReadingEvent records by any field
        distinct
            Filter ReadingEvent records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ReadingEvent
            The first ReadingEvent record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ReadingEvent record ordered by the wordIndex field
        readingevent = await ReadingEvent.prisma().find_first(
            skip=1,
            order={
                'wordIndex': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ReadingEventWhereInput] = None,
        cursor: Optional[types.ReadingEventWhereUniqueInput] = None,
        include: Optional[types.ReadingEventInclude] = None,
        order: Optional[Union[types.ReadingEventOrderByInput, List[types.ReadingEventOrderByInput]]] = None,
        distinct: Optional[List[types.ReadingEventScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ReadingEvent record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ReadingEvent filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ReadingEvent model
        order
            Order the returned ReadingEvent records by any field
        distinct
            Filter ReadingEvent records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ReadingEvent
            The first ReadingEvent record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ReadingEvent record ordered by the paragraphIndex field
        readingevent = await ReadingEvent.prisma().find_first_or_raise(
            skip=1,
            order={
                'paragraphIndex': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ReadingEventUpdateInput,
        where: types.ReadingEventWhereUniqueInput,
        include: Optional[types.ReadingEventInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ReadingEvent record.

        Parameters
        ----------
        data
            ReadingEvent record data specifying what to update
        where
            ReadingEvent filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ReadingEvent model

        Returns
        -------
        prisma.models.ReadingEvent
            The updated ReadingEvent record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        readingevent = await ReadingEvent.prisma().update(
            where={
                'id': 'faajgfadf',
            },
            data={
                # data to update the ReadingEvent record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ReadingEventWhereUniqueInput,
        data: types.ReadingEventUpsertInput,
        include: Optional[types.ReadingEventInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ReadingEvent filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ReadingEvent model

        Returns
        -------
        prisma.models.ReadingEvent
            The created or updated ReadingEvent record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        readingevent = await ReadingEvent.prisma().upsert(
            where={
                'id': 'biaagcedjc',
            },
            data={
                'create': {
                    'id': 'biaagcedjc',
                    'sessionId': 'bdjidcidac',
                    'eventType': 'ifgaaagff',
                },
                'update': {
                    'sessionId': 'bdjidcidac',
                    'eventType': 'ifgaaagff',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ReadingEventUpdateManyMutationInput,
        where: types.ReadingEventWhereInput,
    ) -> int:
        """Update multiple ReadingEvent records

        Parameters
        ----------
        data
            ReadingEvent data to update the selected ReadingEvent records to
        where
            Filter to select the ReadingEvent records to update

        Returns
        -------
        int
            The total number of ReadingEvent records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ReadingEvent records
        total = await ReadingEvent.prisma().update_many(
            data={
                'duration': 2077067425.167211
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReadingEventWhereInput] = None,
        cursor: Optional[types.ReadingEventWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ReadingEvent records present in the database

        Parameters
        ----------
        select
            Select the ReadingEvent fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ReadingEvent filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ReadingEventCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ReadingEvent.prisma().count()

        # results: prisma.types.ReadingEventCountAggregateOutput
        results = await ReadingEvent.prisma().count(
            select={
                '_all': True,
                'text': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ReadingEventCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReadingEventWhereInput] = None,
        cursor: Optional[types.ReadingEventWhereUniqueInput] = None,
    ) -> types.ReadingEventCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ReadingEventCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReadingEventWhereInput] = None,
        cursor: Optional[types.ReadingEventWhereUniqueInput] = None,
    ) -> Union[int, types.ReadingEventCountAggregateOutput]:
        """Count the number of ReadingEvent records present in the database

        Parameters
        ----------
        select
            Select the ReadingEvent fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ReadingEvent filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ReadingEventCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ReadingEvent.prisma().count()

        # results: prisma.types.ReadingEventCountAggregateOutput
        results = await ReadingEvent.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ReadingEventCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ReadingEventWhereInput] = None
    ) -> int:
        """Delete multiple ReadingEvent records.

        Parameters
        ----------
        where
            Optional ReadingEvent filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ReadingEvent records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ReadingEvent records
        total = await ReadingEvent.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ReadingEventScalarFieldKeys'],
        *,
        where: Optional['types.ReadingEventWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ReadingEventAvgAggregateInput'] = None,
        sum: Optional['types.ReadingEventSumAggregateInput'] = None,
        min: Optional['types.ReadingEventMinAggregateInput'] = None,
        max: Optional['types.ReadingEventMaxAggregateInput'] = None,
        having: Optional['types.ReadingEventScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ReadingEventCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ReadingEventScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ReadingEventScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ReadingEventGroupByOutput']:
        """Group ReadingEvent records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ReadingEvent fields to group records by
        where
            ReadingEvent filter to select records
        take
            Limit the maximum number of ReadingEvent records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ReadingEventGroupByOutput]
            A list of dictionaries representing the ReadingEvent record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ReadingEvent records by id values
        # and count how many records are in each group
        results = await ReadingEvent.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class SpellingErrorActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.SpellingError]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await SpellingError.prisma().query_raw(
            'SELECT * FROM SpellingError WHERE id = ?',
            'jcgghhgdj',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.SpellingError
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await SpellingError.prisma().query_first(
            'SELECT * FROM SpellingError WHERE userId = ?',
            'beehgcebbg',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.SpellingErrorCreateInput,
        include: Optional[types.SpellingErrorInclude] = None
    ) -> _PrismaModelT:
        """Create a new SpellingError record.

        Parameters
        ----------
        data
            SpellingError record data
        include
            Specifies which relations should be loaded on the returned SpellingError model

        Returns
        -------
        prisma.models.SpellingError
            The created SpellingError record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a SpellingError record from just the required fields
        spellingerror = await SpellingError.prisma().create(
            data={
                # data to create a SpellingError record
                'userId': 'bhdiaidiaf',
                'errorWord': 'deajegcfi',
                'correctWord': 'gabahhhjf',
                'errorType': 'cjagadcjg',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.SpellingErrorCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple SpellingError records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of SpellingError record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await SpellingError.prisma().create_many(
            data=[
                {
                    # data to create a SpellingError record
                    'userId': 'bifficggej',
                    'errorWord': 'bgbbaajbic',
                    'correctWord': 'eegghdhjb',
                    'errorType': 'daafgidjg',
                },
                {
                    # data to create a SpellingError record
                    'userId': 'gdcgcgagj',
                    'errorWord': 'bhceabbgja',
                    'correctWord': 'ehabfhegh',
                    'errorType': 'bcajcajjbc',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.SpellingErrorWhereUniqueInput,
        include: Optional[types.SpellingErrorInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single SpellingError record.

        Parameters
        ----------
        where
            SpellingError filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned SpellingError model

        Returns
        -------
        prisma.models.SpellingError
            The deleted SpellingError record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        spellingerror = await SpellingError.prisma().delete(
            where={
                'id': 'bfdgheeegf',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.SpellingErrorWhereUniqueInput,
        include: Optional[types.SpellingErrorInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique SpellingError record.

        Parameters
        ----------
        where
            SpellingError filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned SpellingError model

        Returns
        -------
        prisma.models.SpellingError
            The found SpellingError record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        spellingerror = await SpellingError.prisma().find_unique(
            where={
                'id': 'ececbijji',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.SpellingErrorWhereUniqueInput,
        include: Optional[types.SpellingErrorInclude] = None
    ) -> _PrismaModelT:
        """Find a unique SpellingError record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            SpellingError filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned SpellingError model

        Returns
        -------
        prisma.models.SpellingError
            The found SpellingError record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        spellingerror = await SpellingError.prisma().find_unique_or_raise(
            where={
                'id': 'cbcfgdcdhf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SpellingErrorWhereInput] = None,
        cursor: Optional[types.SpellingErrorWhereUniqueInput] = None,
        include: Optional[types.SpellingErrorInclude] = None,
        order: Optional[Union[types.SpellingErrorOrderByInput, List[types.SpellingErrorOrderByInput]]] = None,
        distinct: Optional[List[types.SpellingErrorScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple SpellingError records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of SpellingError records returned
        skip
            Ignore the first N results
        where
            SpellingError filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned SpellingError model
        order
            Order the returned SpellingError records by any field
        distinct
            Filter SpellingError records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.SpellingError]
            The list of all SpellingError records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 SpellingError records
        spellingerrors = await SpellingError.prisma().find_many(take=10)

        # find the first 5 SpellingError records ordered by the errorWord field
        spellingerrors = await SpellingError.prisma().find_many(
            take=5,
            order={
                'errorWord': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SpellingErrorWhereInput] = None,
        cursor: Optional[types.SpellingErrorWhereUniqueInput] = None,
        include: Optional[types.SpellingErrorInclude] = None,
        order: Optional[Union[types.SpellingErrorOrderByInput, List[types.SpellingErrorOrderByInput]]] = None,
        distinct: Optional[List[types.SpellingErrorScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single SpellingError record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            SpellingError filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned SpellingError model
        order
            Order the returned SpellingError records by any field
        distinct
            Filter SpellingError records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.SpellingError
            The first SpellingError record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second SpellingError record ordered by the correctWord field
        spellingerror = await SpellingError.prisma().find_first(
            skip=1,
            order={
                'correctWord': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SpellingErrorWhereInput] = None,
        cursor: Optional[types.SpellingErrorWhereUniqueInput] = None,
        include: Optional[types.SpellingErrorInclude] = None,
        order: Optional[Union[types.SpellingErrorOrderByInput, List[types.SpellingErrorOrderByInput]]] = None,
        distinct: Optional[List[types.SpellingErrorScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single SpellingError record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            SpellingError filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned SpellingError model
        order
            Order the returned SpellingError records by any field
        distinct
            Filter SpellingError records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.SpellingError
            The first SpellingError record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second SpellingError record ordered by the errorType field
        spellingerror = await SpellingError.prisma().find_first_or_raise(
            skip=1,
            order={
                'errorType': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.SpellingErrorUpdateInput,
        where: types.SpellingErrorWhereUniqueInput,
        include: Optional[types.SpellingErrorInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single SpellingError record.

        Parameters
        ----------
        data
            SpellingError record data specifying what to update
        where
            SpellingError filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned SpellingError model

        Returns
        -------
        prisma.models.SpellingError
            The updated SpellingError record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        spellingerror = await SpellingError.prisma().update(
            where={
                'id': 'fdgjfbhia',
            },
            data={
                # data to update the SpellingError record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.SpellingErrorWhereUniqueInput,
        data: types.SpellingErrorUpsertInput,
        include: Optional[types.SpellingErrorInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            SpellingError filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned SpellingError model

        Returns
        -------
        prisma.models.SpellingError
            The created or updated SpellingError record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        spellingerror = await SpellingError.prisma().upsert(
            where={
                'id': 'jcehcdchh',
            },
            data={
                'create': {
                    'id': 'jcehcdchh',
                    'userId': 'gdcgcgagj',
                    'errorWord': 'bhceabbgja',
                    'correctWord': 'ehabfhegh',
                    'errorType': 'bcajcajjbc',
                },
                'update': {
                    'userId': 'gdcgcgagj',
                    'errorWord': 'bhceabbgja',
                    'correctWord': 'ehabfhegh',
                    'errorType': 'bcajcajjbc',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.SpellingErrorUpdateManyMutationInput,
        where: types.SpellingErrorWhereInput,
    ) -> int:
        """Update multiple SpellingError records

        Parameters
        ----------
        data
            SpellingError data to update the selected SpellingError records to
        where
            Filter to select the SpellingError records to update

        Returns
        -------
        int
            The total number of SpellingError records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all SpellingError records
        total = await SpellingError.prisma().update_many(
            data={
                'context': 'bgcbjdhjcc'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SpellingErrorWhereInput] = None,
        cursor: Optional[types.SpellingErrorWhereUniqueInput] = None,
    ) -> int:
        """Count the number of SpellingError records present in the database

        Parameters
        ----------
        select
            Select the SpellingError fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            SpellingError filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SpellingErrorCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await SpellingError.prisma().count()

        # results: prisma.types.SpellingErrorCountAggregateOutput
        results = await SpellingError.prisma().count(
            select={
                '_all': True,
                'timesSeen': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.SpellingErrorCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SpellingErrorWhereInput] = None,
        cursor: Optional[types.SpellingErrorWhereUniqueInput] = None,
    ) -> types.SpellingErrorCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.SpellingErrorCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SpellingErrorWhereInput] = None,
        cursor: Optional[types.SpellingErrorWhereUniqueInput] = None,
    ) -> Union[int, types.SpellingErrorCountAggregateOutput]:
        """Count the number of SpellingError records present in the database

        Parameters
        ----------
        select
            Select the SpellingError fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            SpellingError filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SpellingErrorCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await SpellingError.prisma().count()

        # results: prisma.types.SpellingErrorCountAggregateOutput
        results = await SpellingError.prisma().count(
            select={
                '_all': True,
                'lastSeenAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.SpellingErrorCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.SpellingErrorWhereInput] = None
    ) -> int:
        """Delete multiple SpellingError records.

        Parameters
        ----------
        where
            Optional SpellingError filter to find the records to be deleted

        Returns
        -------
        int
            The total number of SpellingError records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all SpellingError records
        total = await SpellingError.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.SpellingErrorScalarFieldKeys'],
        *,
        where: Optional['types.SpellingErrorWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.SpellingErrorAvgAggregateInput'] = None,
        sum: Optional['types.SpellingErrorSumAggregateInput'] = None,
        min: Optional['types.SpellingErrorMinAggregateInput'] = None,
        max: Optional['types.SpellingErrorMaxAggregateInput'] = None,
        having: Optional['types.SpellingErrorScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.SpellingErrorCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.SpellingErrorScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.SpellingErrorScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.SpellingErrorGroupByOutput']:
        """Group SpellingError records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar SpellingError fields to group records by
        where
            SpellingError filter to select records
        take
            Limit the maximum number of SpellingError records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.SpellingErrorGroupByOutput]
            A list of dictionaries representing the SpellingError record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group SpellingError records by masteredAt values
        # and count how many records are in each group
        results = await SpellingError.prisma().group_by(
            ['masteredAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class UserProfileActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.UserProfile]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await UserProfile.prisma().query_raw(
            'SELECT * FROM UserProfile WHERE id = ?',
            'bieiidcabj',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.UserProfile
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await UserProfile.prisma().query_first(
            'SELECT * FROM UserProfile WHERE userId = ?',
            'bjcbfcieaa',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.UserProfileCreateInput,
        include: Optional[types.UserProfileInclude] = None
    ) -> _PrismaModelT:
        """Create a new UserProfile record.

        Parameters
        ----------
        data
            UserProfile record data
        include
            Specifies which relations should be loaded on the returned UserProfile model

        Returns
        -------
        prisma.models.UserProfile
            The created UserProfile record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a UserProfile record from just the required fields
        userprofile = await UserProfile.prisma().create(
            data={
                # data to create a UserProfile record
                'userId': 'cbaaechiej',
                'commonErrors': 'iejbeaaeg',
                'strugglingPatterns': 'jcibfcbhf',
                'targetExercises': 'chdadcaga',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.UserProfileCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple UserProfile records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of UserProfile record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await UserProfile.prisma().create_many(
            data=[
                {
                    # data to create a UserProfile record
                    'userId': 'jicieifbh',
                    'commonErrors': 'fbahdheji',
                    'strugglingPatterns': 'cbbheiicgh',
                    'targetExercises': 'beabjeejdg',
                },
                {
                    # data to create a UserProfile record
                    'userId': 'bcjhgahffd',
                    'commonErrors': 'fbjeiiffa',
                    'strugglingPatterns': 'jhgidcgbf',
                    'targetExercises': 'bgjgecfejc',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.UserProfileWhereUniqueInput,
        include: Optional[types.UserProfileInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single UserProfile record.

        Parameters
        ----------
        where
            UserProfile filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned UserProfile model

        Returns
        -------
        prisma.models.UserProfile
            The deleted UserProfile record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userprofile = await UserProfile.prisma().delete(
            where={
                'id': 'bgjcgchib',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.UserProfileWhereUniqueInput,
        include: Optional[types.UserProfileInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique UserProfile record.

        Parameters
        ----------
        where
            UserProfile filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned UserProfile model

        Returns
        -------
        prisma.models.UserProfile
            The found UserProfile record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userprofile = await UserProfile.prisma().find_unique(
            where={
                'id': 'bacdaibgfa',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.UserProfileWhereUniqueInput,
        include: Optional[types.UserProfileInclude] = None
    ) -> _PrismaModelT:
        """Find a unique UserProfile record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            UserProfile filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned UserProfile model

        Returns
        -------
        prisma.models.UserProfile
            The found UserProfile record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userprofile = await UserProfile.prisma().find_unique_or_raise(
            where={
                'id': 'dchgibach',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserProfileWhereInput] = None,
        cursor: Optional[types.UserProfileWhereUniqueInput] = None,
        include: Optional[types.UserProfileInclude] = None,
        order: Optional[Union[types.UserProfileOrderByInput, List[types.UserProfileOrderByInput]]] = None,
        distinct: Optional[List[types.UserProfileScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple UserProfile records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of UserProfile records returned
        skip
            Ignore the first N results
        where
            UserProfile filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned UserProfile model
        order
            Order the returned UserProfile records by any field
        distinct
            Filter UserProfile records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.UserProfile]
            The list of all UserProfile records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 UserProfile records
        userprofiles = await UserProfile.prisma().find_many(take=10)

        # find the first 5 UserProfile records ordered by the commonErrors field
        userprofiles = await UserProfile.prisma().find_many(
            take=5,
            order={
                'commonErrors': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.UserProfileWhereInput] = None,
        cursor: Optional[types.UserProfileWhereUniqueInput] = None,
        include: Optional[types.UserProfileInclude] = None,
        order: Optional[Union[types.UserProfileOrderByInput, List[types.UserProfileOrderByInput]]] = None,
        distinct: Optional[List[types.UserProfileScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single UserProfile record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            UserProfile filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned UserProfile model
        order
            Order the returned UserProfile records by any field
        distinct
            Filter UserProfile records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.UserProfile
            The first UserProfile record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second UserProfile record ordered by the strugglingPatterns field
        userprofile = await UserProfile.prisma().find_first(
            skip=1,
            order={
                'strugglingPatterns': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.UserProfileWhereInput] = None,
        cursor: Optional[types.UserProfileWhereUniqueInput] = None,
        include: Optional[types.UserProfileInclude] = None,
        order: Optional[Union[types.UserProfileOrderByInput, List[types.UserProfileOrderByInput]]] = None,
        distinct: Optional[List[types.UserProfileScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single UserProfile record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            UserProfile filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned UserProfile model
        order
            Order the returned UserProfile records by any field
        distinct
            Filter UserProfile records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.UserProfile
            The first UserProfile record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second UserProfile record ordered by the averageDifficulty field
        userprofile = await UserProfile.prisma().find_first_or_raise(
            skip=1,
            order={
                'averageDifficulty': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.UserProfileUpdateInput,
        where: types.UserProfileWhereUniqueInput,
        include: Optional[types.UserProfileInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single UserProfile record.

        Parameters
        ----------
        data
            UserProfile record data specifying what to update
        where
            UserProfile filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned UserProfile model

        Returns
        -------
        prisma.models.UserProfile
            The updated UserProfile record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        userprofile = await UserProfile.prisma().update(
            where={
                'id': 'fchheijjc',
            },
            data={
                # data to update the UserProfile record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.UserProfileWhereUniqueInput,
        data: types.UserProfileUpsertInput,
        include: Optional[types.UserProfileInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            UserProfile filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned UserProfile model

        Returns
        -------
        prisma.models.UserProfile
            The created or updated UserProfile record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userprofile = await UserProfile.prisma().upsert(
            where={
                'id': 'cacjdfhejh',
            },
            data={
                'create': {
                    'id': 'cacjdfhejh',
                    'userId': 'bcjhgahffd',
                    'commonErrors': 'fbjeiiffa',
                    'strugglingPatterns': 'jhgidcgbf',
                    'targetExercises': 'bgjgecfejc',
                },
                'update': {
                    'userId': 'bcjhgahffd',
                    'commonErrors': 'fbjeiiffa',
                    'strugglingPatterns': 'jhgidcgbf',
                    'targetExercises': 'bgjgecfejc',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.UserProfileUpdateManyMutationInput,
        where: types.UserProfileWhereInput,
    ) -> int:
        """Update multiple UserProfile records

        Parameters
        ----------
        data
            UserProfile data to update the selected UserProfile records to
        where
            Filter to select the UserProfile records to update

        Returns
        -------
        int
            The total number of UserProfile records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all UserProfile records
        total = await UserProfile.prisma().update_many(
            data={
                'targetExercises': 'bdbifjhbbi'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserProfileWhereInput] = None,
        cursor: Optional[types.UserProfileWhereUniqueInput] = None,
    ) -> int:
        """Count the number of UserProfile records present in the database

        Parameters
        ----------
        select
            Select the UserProfile fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            UserProfile filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.UserProfileCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await UserProfile.prisma().count()

        # results: prisma.types.UserProfileCountAggregateOutput
        results = await UserProfile.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.UserProfileCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserProfileWhereInput] = None,
        cursor: Optional[types.UserProfileWhereUniqueInput] = None,
    ) -> types.UserProfileCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.UserProfileCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserProfileWhereInput] = None,
        cursor: Optional[types.UserProfileWhereUniqueInput] = None,
    ) -> Union[int, types.UserProfileCountAggregateOutput]:
        """Count the number of UserProfile records present in the database

        Parameters
        ----------
        select
            Select the UserProfile fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            UserProfile filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.UserProfileCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await UserProfile.prisma().count()

        # results: prisma.types.UserProfileCountAggregateOutput
        results = await UserProfile.prisma().count(
            select={
                '_all': True,
                'updatedAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.UserProfileCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.UserProfileWhereInput] = None
    ) -> int:
        """Delete multiple UserProfile records.

        Parameters
        ----------
        where
            Optional UserProfile filter to find the records to be deleted

        Returns
        -------
        int
            The total number of UserProfile records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all UserProfile records
        total = await UserProfile.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.UserProfileScalarFieldKeys'],
        *,
        where: Optional['types.UserProfileWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.UserProfileAvgAggregateInput'] = None,
        sum: Optional['types.UserProfileSumAggregateInput'] = None,
        min: Optional['types.UserProfileMinAggregateInput'] = None,
        max: Optional['types.UserProfileMaxAggregateInput'] = None,
        having: Optional['types.UserProfileScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.UserProfileCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.UserProfileScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.UserProfileScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.UserProfileGroupByOutput']:
        """Group UserProfile records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar UserProfile fields to group records by
        where
            UserProfile filter to select records
        take
            Limit the maximum number of UserProfile records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.UserProfileGroupByOutput]
            A list of dictionaries representing the UserProfile record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group UserProfile records by id values
        # and count how many records are in each group
        results = await UserProfile.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class WordDifficultyActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.WordDifficulty]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await WordDifficulty.prisma().query_raw(
            'SELECT * FROM WordDifficulty WHERE id = ?',
            'cbccbbcdfb',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.WordDifficulty
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await WordDifficulty.prisma().query_first(
            'SELECT * FROM WordDifficulty WHERE word = ?',
            'bacejedaca',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.WordDifficultyCreateInput,
        include: Optional[types.WordDifficultyInclude] = None
    ) -> _PrismaModelT:
        """Create a new WordDifficulty record.

        Parameters
        ----------
        data
            WordDifficulty record data
        include
            Specifies which relations should be loaded on the returned WordDifficulty model

        Returns
        -------
        prisma.models.WordDifficulty
            The created WordDifficulty record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a WordDifficulty record from just the required fields
        worddifficulty = await WordDifficulty.prisma().create(
            data={
                # data to create a WordDifficulty record
                'word': 'bhbhdahfaj',
                'length': 1598124042,
                'syllableCount': 817623163,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.WordDifficultyCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple WordDifficulty records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of WordDifficulty record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await WordDifficulty.prisma().create_many(
            data=[
                {
                    # data to create a WordDifficulty record
                    'word': 'badaffhddg',
                    'length': 1131525873,
                    'syllableCount': 210666198,
                },
                {
                    # data to create a WordDifficulty record
                    'word': 'bchgafhjed',
                    'length': 745569348,
                    'syllableCount': 307876141,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.WordDifficultyWhereUniqueInput,
        include: Optional[types.WordDifficultyInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single WordDifficulty record.

        Parameters
        ----------
        where
            WordDifficulty filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned WordDifficulty model

        Returns
        -------
        prisma.models.WordDifficulty
            The deleted WordDifficulty record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        worddifficulty = await WordDifficulty.prisma().delete(
            where={
                'id': 'bgheaejbcc',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.WordDifficultyWhereUniqueInput,
        include: Optional[types.WordDifficultyInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique WordDifficulty record.

        Parameters
        ----------
        where
            WordDifficulty filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned WordDifficulty model

        Returns
        -------
        prisma.models.WordDifficulty
            The found WordDifficulty record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        worddifficulty = await WordDifficulty.prisma().find_unique(
            where={
                'id': 'bfcgifeged',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.WordDifficultyWhereUniqueInput,
        include: Optional[types.WordDifficultyInclude] = None
    ) -> _PrismaModelT:
        """Find a unique WordDifficulty record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            WordDifficulty filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned WordDifficulty model

        Returns
        -------
        prisma.models.WordDifficulty
            The found WordDifficulty record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        worddifficulty = await WordDifficulty.prisma().find_unique_or_raise(
            where={
                'id': 'jfiahhbae',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.WordDifficultyWhereInput] = None,
        cursor: Optional[types.WordDifficultyWhereUniqueInput] = None,
        include: Optional[types.WordDifficultyInclude] = None,
        order: Optional[Union[types.WordDifficultyOrderByInput, List[types.WordDifficultyOrderByInput]]] = None,
        distinct: Optional[List[types.WordDifficultyScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple WordDifficulty records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of WordDifficulty records returned
        skip
            Ignore the first N results
        where
            WordDifficulty filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned WordDifficulty model
        order
            Order the returned WordDifficulty records by any field
        distinct
            Filter WordDifficulty records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.WordDifficulty]
            The list of all WordDifficulty records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 WordDifficulty records
        worddifficultys = await WordDifficulty.prisma().find_many(take=10)

        # find the first 5 WordDifficulty records ordered by the length field
        worddifficultys = await WordDifficulty.prisma().find_many(
            take=5,
            order={
                'length': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.WordDifficultyWhereInput] = None,
        cursor: Optional[types.WordDifficultyWhereUniqueInput] = None,
        include: Optional[types.WordDifficultyInclude] = None,
        order: Optional[Union[types.WordDifficultyOrderByInput, List[types.WordDifficultyOrderByInput]]] = None,
        distinct: Optional[List[types.WordDifficultyScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single WordDifficulty record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            WordDifficulty filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned WordDifficulty model
        order
            Order the returned WordDifficulty records by any field
        distinct
            Filter WordDifficulty records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.WordDifficulty
            The first WordDifficulty record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second WordDifficulty record ordered by the syllableCount field
        worddifficulty = await WordDifficulty.prisma().find_first(
            skip=1,
            order={
                'syllableCount': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.WordDifficultyWhereInput] = None,
        cursor: Optional[types.WordDifficultyWhereUniqueInput] = None,
        include: Optional[types.WordDifficultyInclude] = None,
        order: Optional[Union[types.WordDifficultyOrderByInput, List[types.WordDifficultyOrderByInput]]] = None,
        distinct: Optional[List[types.WordDifficultyScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single WordDifficulty record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            WordDifficulty filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned WordDifficulty model
        order
            Order the returned WordDifficulty records by any field
        distinct
            Filter WordDifficulty records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.WordDifficulty
            The first WordDifficulty record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second WordDifficulty record ordered by the phoneticScore field
        worddifficulty = await WordDifficulty.prisma().find_first_or_raise(
            skip=1,
            order={
                'phoneticScore': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.WordDifficultyUpdateInput,
        where: types.WordDifficultyWhereUniqueInput,
        include: Optional[types.WordDifficultyInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single WordDifficulty record.

        Parameters
        ----------
        data
            WordDifficulty record data specifying what to update
        where
            WordDifficulty filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned WordDifficulty model

        Returns
        -------
        prisma.models.WordDifficulty
            The updated WordDifficulty record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        worddifficulty = await WordDifficulty.prisma().update(
            where={
                'id': 'bfbdafajcb',
            },
            data={
                # data to update the WordDifficulty record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.WordDifficultyWhereUniqueInput,
        data: types.WordDifficultyUpsertInput,
        include: Optional[types.WordDifficultyInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            WordDifficulty filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned WordDifficulty model

        Returns
        -------
        prisma.models.WordDifficulty
            The created or updated WordDifficulty record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        worddifficulty = await WordDifficulty.prisma().upsert(
            where={
                'id': 'caeghehde',
            },
            data={
                'create': {
                    'id': 'caeghehde',
                    'word': 'bchgafhjed',
                    'length': 745569348,
                    'syllableCount': 307876141,
                },
                'update': {
                    'word': 'bchgafhjed',
                    'length': 745569348,
                    'syllableCount': 307876141,
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.WordDifficultyUpdateManyMutationInput,
        where: types.WordDifficultyWhereInput,
    ) -> int:
        """Update multiple WordDifficulty records

        Parameters
        ----------
        data
            WordDifficulty data to update the selected WordDifficulty records to
        where
            Filter to select the WordDifficulty records to update

        Returns
        -------
        int
            The total number of WordDifficulty records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all WordDifficulty records
        total = await WordDifficulty.prisma().update_many(
            data={
                'globalErrorRate': 2067651663.118391
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.WordDifficultyWhereInput] = None,
        cursor: Optional[types.WordDifficultyWhereUniqueInput] = None,
    ) -> int:
        """Count the number of WordDifficulty records present in the database

        Parameters
        ----------
        select
            Select the WordDifficulty fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            WordDifficulty filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.WordDifficultyCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await WordDifficulty.prisma().count()

        # results: prisma.types.WordDifficultyCountAggregateOutput
        results = await WordDifficulty.prisma().count(
            select={
                '_all': True,
                'difficultyScore': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.WordDifficultyCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.WordDifficultyWhereInput] = None,
        cursor: Optional[types.WordDifficultyWhereUniqueInput] = None,
    ) -> types.WordDifficultyCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.WordDifficultyCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.WordDifficultyWhereInput] = None,
        cursor: Optional[types.WordDifficultyWhereUniqueInput] = None,
    ) -> Union[int, types.WordDifficultyCountAggregateOutput]:
        """Count the number of WordDifficulty records present in the database

        Parameters
        ----------
        select
            Select the WordDifficulty fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            WordDifficulty filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.WordDifficultyCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await WordDifficulty.prisma().count()

        # results: prisma.types.WordDifficultyCountAggregateOutput
        results = await WordDifficulty.prisma().count(
            select={
                '_all': True,
                'pronunciation': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.WordDifficultyCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.WordDifficultyWhereInput] = None
    ) -> int:
        """Delete multiple WordDifficulty records.

        Parameters
        ----------
        where
            Optional WordDifficulty filter to find the records to be deleted

        Returns
        -------
        int
            The total number of WordDifficulty records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all WordDifficulty records
        total = await WordDifficulty.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.WordDifficultyScalarFieldKeys'],
        *,
        where: Optional['types.WordDifficultyWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.WordDifficultyAvgAggregateInput'] = None,
        sum: Optional['types.WordDifficultySumAggregateInput'] = None,
        min: Optional['types.WordDifficultyMinAggregateInput'] = None,
        max: Optional['types.WordDifficultyMaxAggregateInput'] = None,
        having: Optional['types.WordDifficultyScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.WordDifficultyCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.WordDifficultyScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.WordDifficultyScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.WordDifficultyGroupByOutput']:
        """Group WordDifficulty records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar WordDifficulty fields to group records by
        where
            WordDifficulty filter to select records
        take
            Limit the maximum number of WordDifficulty records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.WordDifficultyGroupByOutput]
            A list of dictionaries representing the WordDifficulty record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group WordDifficulty records by simpleMeaning values
        # and count how many records are in each group
        results = await WordDifficulty.prisma().group_by(
            ['simpleMeaning'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class UserWordHistoryActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.UserWordHistory]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await UserWordHistory.prisma().query_raw(
            'SELECT * FROM UserWordHistory WHERE id = ?',
            'bfijhaejdd',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.UserWordHistory
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await UserWordHistory.prisma().query_first(
            'SELECT * FROM UserWordHistory WHERE userId = ?',
            'bcedehfiji',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.UserWordHistoryCreateInput,
        include: Optional[types.UserWordHistoryInclude] = None
    ) -> _PrismaModelT:
        """Create a new UserWordHistory record.

        Parameters
        ----------
        data
            UserWordHistory record data
        include
            Specifies which relations should be loaded on the returned UserWordHistory model

        Returns
        -------
        prisma.models.UserWordHistory
            The created UserWordHistory record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a UserWordHistory record from just the required fields
        userwordhistory = await UserWordHistory.prisma().create(
            data={
                # data to create a UserWordHistory record
                'userId': 'bdgjicijhb',
                'wordId': 'bghifjdeia',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.UserWordHistoryCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple UserWordHistory records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of UserWordHistory record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await UserWordHistory.prisma().create_many(
            data=[
                {
                    # data to create a UserWordHistory record
                    'userId': 'eadfcbbcb',
                    'wordId': 'geihgahba',
                },
                {
                    # data to create a UserWordHistory record
                    'userId': 'gahdcdhbj',
                    'wordId': 'begiijahea',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.UserWordHistoryWhereUniqueInput,
        include: Optional[types.UserWordHistoryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single UserWordHistory record.

        Parameters
        ----------
        where
            UserWordHistory filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned UserWordHistory model

        Returns
        -------
        prisma.models.UserWordHistory
            The deleted UserWordHistory record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userwordhistory = await UserWordHistory.prisma().delete(
            where={
                'id': 'gcjadjaaf',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.UserWordHistoryWhereUniqueInput,
        include: Optional[types.UserWordHistoryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique UserWordHistory record.

        Parameters
        ----------
        where
            UserWordHistory filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned UserWordHistory model

        Returns
        -------
        prisma.models.UserWordHistory
            The found UserWordHistory record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userwordhistory = await UserWordHistory.prisma().find_unique(
            where={
                'id': 'bcbebgiaic',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.UserWordHistoryWhereUniqueInput,
        include: Optional[types.UserWordHistoryInclude] = None
    ) -> _PrismaModelT:
        """Find a unique UserWordHistory record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            UserWordHistory filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned UserWordHistory model

        Returns
        -------
        prisma.models.UserWordHistory
            The found UserWordHistory record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userwordhistory = await UserWordHistory.prisma().find_unique_or_raise(
            where={
                'id': 'ijigbdcbj',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWordHistoryWhereInput] = None,
        cursor: Optional[types.UserWordHistoryWhereUniqueInput] = None,
        include: Optional[types.UserWordHistoryInclude] = None,
        order: Optional[Union[types.UserWordHistoryOrderByInput, List[types.UserWordHistoryOrderByInput]]] = None,
        distinct: Optional[List[types.UserWordHistoryScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple UserWordHistory records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of UserWordHistory records returned
        skip
            Ignore the first N results
        where
            UserWordHistory filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned UserWordHistory model
        order
            Order the returned UserWordHistory records by any field
        distinct
            Filter UserWordHistory records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.UserWordHistory]
            The list of all UserWordHistory records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 UserWordHistory records
        userwordhistorys = await UserWordHistory.prisma().find_many(take=10)

        # find the first 5 UserWordHistory records ordered by the wordId field
        userwordhistorys = await UserWordHistory.prisma().find_many(
            take=5,
            order={
                'wordId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.UserWordHistoryWhereInput] = None,
        cursor: Optional[types.UserWordHistoryWhereUniqueInput] = None,
        include: Optional[types.UserWordHistoryInclude] = None,
        order: Optional[Union[types.UserWordHistoryOrderByInput, List[types.UserWordHistoryOrderByInput]]] = None,
        distinct: Optional[List[types.UserWordHistoryScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single UserWordHistory record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            UserWordHistory filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned UserWordHistory model
        order
            Order the returned UserWordHistory records by any field
        distinct
            Filter UserWordHistory records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.UserWordHistory
            The first UserWordHistory record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second UserWordHistory record ordered by the timesSeen field
        userwordhistory = await UserWordHistory.prisma().find_first(
            skip=1,
            order={
                'timesSeen': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.UserWordHistoryWhereInput] = None,
        cursor: Optional[types.UserWordHistoryWhereUniqueInput] = None,
        include: Optional[types.UserWordHistoryInclude] = None,
        order: Optional[Union[types.UserWordHistoryOrderByInput, List[types.UserWordHistoryOrderByInput]]] = None,
        distinct: Optional[List[types.UserWordHistoryScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single UserWordHistory record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            UserWordHistory filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned UserWordHistory model
        order
            Order the returned UserWordHistory records by any field
        distinct
            Filter UserWordHistory records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.UserWordHistory
            The first UserWordHistory record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second UserWordHistory record ordered by the timesCorrect field
        userwordhistory = await UserWordHistory.prisma().find_first_or_raise(
            skip=1,
            order={
                'timesCorrect': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.UserWordHistoryUpdateInput,
        where: types.UserWordHistoryWhereUniqueInput,
        include: Optional[types.UserWordHistoryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single UserWordHistory record.

        Parameters
        ----------
        data
            UserWordHistory record data specifying what to update
        where
            UserWordHistory filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned UserWordHistory model

        Returns
        -------
        prisma.models.UserWordHistory
            The updated UserWordHistory record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        userwordhistory = await UserWordHistory.prisma().update(
            where={
                'id': 'gfidhicai',
            },
            data={
                # data to update the UserWordHistory record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.UserWordHistoryWhereUniqueInput,
        data: types.UserWordHistoryUpsertInput,
        include: Optional[types.UserWordHistoryInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            UserWordHistory filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned UserWordHistory model

        Returns
        -------
        prisma.models.UserWordHistory
            The created or updated UserWordHistory record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        userwordhistory = await UserWordHistory.prisma().upsert(
            where={
                'id': 'jfegcaafh',
            },
            data={
                'create': {
                    'id': 'jfegcaafh',
                    'userId': 'gahdcdhbj',
                    'wordId': 'begiijahea',
                },
                'update': {
                    'userId': 'gahdcdhbj',
                    'wordId': 'begiijahea',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.UserWordHistoryUpdateManyMutationInput,
        where: types.UserWordHistoryWhereInput,
    ) -> int:
        """Update multiple UserWordHistory records

        Parameters
        ----------
        data
            UserWordHistory data to update the selected UserWordHistory records to
        where
            Filter to select the UserWordHistory records to update

        Returns
        -------
        int
            The total number of UserWordHistory records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all UserWordHistory records
        total = await UserWordHistory.prisma().update_many(
            data={
                'timesErrored': 1214809950
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWordHistoryWhereInput] = None,
        cursor: Optional[types.UserWordHistoryWhereUniqueInput] = None,
    ) -> int:
        """Count the number of UserWordHistory records present in the database

        Parameters
        ----------
        select
            Select the UserWordHistory fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            UserWordHistory filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.UserWordHistoryCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await UserWordHistory.prisma().count()

        # results: prisma.types.UserWordHistoryCountAggregateOutput
        results = await UserWordHistory.prisma().count(
            select={
                '_all': True,
                'lastSeenAt': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.UserWordHistoryCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWordHistoryWhereInput] = None,
        cursor: Optional[types.UserWordHistoryWhereUniqueInput] = None,
    ) -> types.UserWordHistoryCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.UserWordHistoryCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.UserWordHistoryWhereInput] = None,
        cursor: Optional[types.UserWordHistoryWhereUniqueInput] = None,
    ) -> Union[int, types.UserWordHistoryCountAggregateOutput]:
        """Count the number of UserWordHistory records present in the database

        Parameters
        ----------
        select
            Select the UserWordHistory fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            UserWordHistory filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.UserWordHistoryCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await UserWordHistory.prisma().count()

        # results: prisma.types.UserWordHistoryCountAggregateOutput
        results = await UserWordHistory.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.UserWordHistoryCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.UserWordHistoryWhereInput] = None
    ) -> int:
        """Delete multiple UserWordHistory records.

        Parameters
        ----------
        where
            Optional UserWordHistory filter to find the records to be deleted

        Returns
        -------
        int
            The total number of UserWordHistory records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all UserWordHistory records
        total = await UserWordHistory.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.UserWordHistoryScalarFieldKeys'],
        *,
        where: Optional['types.UserWordHistoryWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.UserWordHistoryAvgAggregateInput'] = None,
        sum: Optional['types.UserWordHistorySumAggregateInput'] = None,
        min: Optional['types.UserWordHistoryMinAggregateInput'] = None,
        max: Optional['types.UserWordHistoryMaxAggregateInput'] = None,
        having: Optional['types.UserWordHistoryScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.UserWordHistoryCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.UserWordHistoryScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.UserWordHistoryScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.UserWordHistoryGroupByOutput']:
        """Group UserWordHistory records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar UserWordHistory fields to group records by
        where
            UserWordHistory filter to select records
        take
            Limit the maximum number of UserWordHistory records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.UserWordHistoryGroupByOutput]
            A list of dictionaries representing the UserWordHistory record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group UserWordHistory records by updatedAt values
        # and count how many records are in each group
        results = await UserWordHistory.prisma().group_by(
            ['updatedAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ConceptDiagramActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ConceptDiagram]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await ConceptDiagram.prisma().query_raw(
            'SELECT * FROM ConceptDiagram WHERE id = ?',
            'baehicaajf',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ConceptDiagram
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await ConceptDiagram.prisma().query_first(
            'SELECT * FROM ConceptDiagram WHERE userId = ?',
            'bdachdeiga',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ConceptDiagramCreateInput,
        include: Optional[types.ConceptDiagramInclude] = None
    ) -> _PrismaModelT:
        """Create a new ConceptDiagram record.

        Parameters
        ----------
        data
            ConceptDiagram record data
        include
            Specifies which relations should be loaded on the returned ConceptDiagram model

        Returns
        -------
        prisma.models.ConceptDiagram
            The created ConceptDiagram record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ConceptDiagram record from just the required fields
        conceptdiagram = await ConceptDiagram.prisma().create(
            data={
                # data to create a ConceptDiagram record
                'userId': 'ijdafccef',
                'conceptName': 'ciaaiddag',
                'description': 'fejggijff',
                'nodes': 'hghjaaai',
                'edges': 'cajicjjdef',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ConceptDiagramCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ConceptDiagram records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ConceptDiagram record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await ConceptDiagram.prisma().create_many(
            data=[
                {
                    # data to create a ConceptDiagram record
                    'userId': 'cefjaadec',
                    'conceptName': 'ibbigdigd',
                    'description': 'bdiiiabbii',
                    'nodes': 'hfcfhhadh',
                    'edges': 'bbihggdcji',
                },
                {
                    # data to create a ConceptDiagram record
                    'userId': 'hgjgibdgd',
                    'conceptName': 'bcbecjfice',
                    'description': 'bacbebhjjd',
                    'nodes': 'dfbfaddhe',
                    'edges': 'bdcbbieibf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ConceptDiagramWhereUniqueInput,
        include: Optional[types.ConceptDiagramInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ConceptDiagram record.

        Parameters
        ----------
        where
            ConceptDiagram filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ConceptDiagram model

        Returns
        -------
        prisma.models.ConceptDiagram
            The deleted ConceptDiagram record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        conceptdiagram = await ConceptDiagram.prisma().delete(
            where={
                'id': 'dgjhdcggi',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ConceptDiagramWhereUniqueInput,
        include: Optional[types.ConceptDiagramInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ConceptDiagram record.

        Parameters
        ----------
        where
            ConceptDiagram filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ConceptDiagram model

        Returns
        -------
        prisma.models.ConceptDiagram
            The found ConceptDiagram record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        conceptdiagram = await ConceptDiagram.prisma().find_unique(
            where={
                'id': 'bbjbcdfabd',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ConceptDiagramWhereUniqueInput,
        include: Optional[types.ConceptDiagramInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ConceptDiagram record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ConceptDiagram filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ConceptDiagram model

        Returns
        -------
        prisma.models.ConceptDiagram
            The found ConceptDiagram record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        conceptdiagram = await ConceptDiagram.prisma().find_unique_or_raise(
            where={
                'id': 'gchfgbcec',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ConceptDiagramWhereInput] = None,
        cursor: Optional[types.ConceptDiagramWhereUniqueInput] = None,
        include: Optional[types.ConceptDiagramInclude] = None,
        order: Optional[Union[types.ConceptDiagramOrderByInput, List[types.ConceptDiagramOrderByInput]]] = None,
        distinct: Optional[List[types.ConceptDiagramScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ConceptDiagram records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ConceptDiagram records returned
        skip
            Ignore the first N results
        where
            ConceptDiagram filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ConceptDiagram model
        order
            Order the returned ConceptDiagram records by any field
        distinct
            Filter ConceptDiagram records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ConceptDiagram]
            The list of all ConceptDiagram records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ConceptDiagram records
        conceptdiagrams = await ConceptDiagram.prisma().find_many(take=10)

        # find the first 5 ConceptDiagram records ordered by the conceptName field
        conceptdiagrams = await ConceptDiagram.prisma().find_many(
            take=5,
            order={
                'conceptName': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ConceptDiagramWhereInput] = None,
        cursor: Optional[types.ConceptDiagramWhereUniqueInput] = None,
        include: Optional[types.ConceptDiagramInclude] = None,
        order: Optional[Union[types.ConceptDiagramOrderByInput, List[types.ConceptDiagramOrderByInput]]] = None,
        distinct: Optional[List[types.ConceptDiagramScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ConceptDiagram record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ConceptDiagram filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ConceptDiagram model
        order
            Order the returned ConceptDiagram records by any field
        distinct
            Filter ConceptDiagram records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ConceptDiagram
            The first ConceptDiagram record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ConceptDiagram record ordered by the description field
        conceptdiagram = await ConceptDiagram.prisma().find_first(
            skip=1,
            order={
                'description': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ConceptDiagramWhereInput] = None,
        cursor: Optional[types.ConceptDiagramWhereUniqueInput] = None,
        include: Optional[types.ConceptDiagramInclude] = None,
        order: Optional[Union[types.ConceptDiagramOrderByInput, List[types.ConceptDiagramOrderByInput]]] = None,
        distinct: Optional[List[types.ConceptDiagramScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ConceptDiagram record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ConceptDiagram filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ConceptDiagram model
        order
            Order the returned ConceptDiagram records by any field
        distinct
            Filter ConceptDiagram records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ConceptDiagram
            The first ConceptDiagram record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ConceptDiagram record ordered by the nodes field
        conceptdiagram = await ConceptDiagram.prisma().find_first_or_raise(
            skip=1,
            order={
                'nodes': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ConceptDiagramUpdateInput,
        where: types.ConceptDiagramWhereUniqueInput,
        include: Optional[types.ConceptDiagramInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ConceptDiagram record.

        Parameters
        ----------
        data
            ConceptDiagram record data specifying what to update
        where
            ConceptDiagram filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ConceptDiagram model

        Returns
        -------
        prisma.models.ConceptDiagram
            The updated ConceptDiagram record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        conceptdiagram = await ConceptDiagram.prisma().update(
            where={
                'id': 'bihcjfcjah',
            },
            data={
                # data to update the ConceptDiagram record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ConceptDiagramWhereUniqueInput,
        data: types.ConceptDiagramUpsertInput,
        include: Optional[types.ConceptDiagramInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ConceptDiagram filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ConceptDiagram model

        Returns
        -------
        prisma.models.ConceptDiagram
            The created or updated ConceptDiagram record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        conceptdiagram = await ConceptDiagram.prisma().upsert(
            where={
                'id': 'bhjdcicaii',
            },
            data={
                'create': {
                    'id': 'bhjdcicaii',
                    'userId': 'hgjgibdgd',
                    'conceptName': 'bcbecjfice',
                    'description': 'bacbebhjjd',
                    'nodes': 'dfbfaddhe',
                    'edges': 'bdcbbieibf',
                },
                'update': {
                    'userId': 'hgjgibdgd',
                    'conceptName': 'bcbecjfice',
                    'description': 'bacbebhjjd',
                    'nodes': 'dfbfaddhe',
                    'edges': 'bdcbbieibf',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ConceptDiagramUpdateManyMutationInput,
        where: types.ConceptDiagramWhereInput,
    ) -> int:
        """Update multiple ConceptDiagram records

        Parameters
        ----------
        data
            ConceptDiagram data to update the selected ConceptDiagram records to
        where
            Filter to select the ConceptDiagram records to update

        Returns
        -------
        int
            The total number of ConceptDiagram records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ConceptDiagram records
        total = await ConceptDiagram.prisma().update_many(
            data={
                'edges': 'bibedjhcej'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ConceptDiagramWhereInput] = None,
        cursor: Optional[types.ConceptDiagramWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ConceptDiagram records present in the database

        Parameters
        ----------
        select
            Select the ConceptDiagram fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ConceptDiagram filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ConceptDiagramCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ConceptDiagram.prisma().count()

        # results: prisma.types.ConceptDiagramCountAggregateOutput
        results = await ConceptDiagram.prisma().count(
            select={
                '_all': True,
                'generatedAt': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ConceptDiagramCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ConceptDiagramWhereInput] = None,
        cursor: Optional[types.ConceptDiagramWhereUniqueInput] = None,
    ) -> types.ConceptDiagramCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ConceptDiagramCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ConceptDiagramWhereInput] = None,
        cursor: Optional[types.ConceptDiagramWhereUniqueInput] = None,
    ) -> Union[int, types.ConceptDiagramCountAggregateOutput]:
        """Count the number of ConceptDiagram records present in the database

        Parameters
        ----------
        select
            Select the ConceptDiagram fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ConceptDiagram filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ConceptDiagramCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ConceptDiagram.prisma().count()

        # results: prisma.types.ConceptDiagramCountAggregateOutput
        results = await ConceptDiagram.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ConceptDiagramCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ConceptDiagramWhereInput] = None
    ) -> int:
        """Delete multiple ConceptDiagram records.

        Parameters
        ----------
        where
            Optional ConceptDiagram filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ConceptDiagram records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ConceptDiagram records
        total = await ConceptDiagram.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ConceptDiagramScalarFieldKeys'],
        *,
        where: Optional['types.ConceptDiagramWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ConceptDiagramAvgAggregateInput'] = None,
        sum: Optional['types.ConceptDiagramSumAggregateInput'] = None,
        min: Optional['types.ConceptDiagramMinAggregateInput'] = None,
        max: Optional['types.ConceptDiagramMaxAggregateInput'] = None,
        having: Optional['types.ConceptDiagramScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ConceptDiagramCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ConceptDiagramScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ConceptDiagramScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ConceptDiagramGroupByOutput']:
        """Group ConceptDiagram records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ConceptDiagram fields to group records by
        where
            ConceptDiagram filter to select records
        take
            Limit the maximum number of ConceptDiagram records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ConceptDiagramGroupByOutput]
            A list of dictionaries representing the ConceptDiagram record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ConceptDiagram records by id values
        # and count how many records are in each group
        results = await ConceptDiagram.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class DailyProgressActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.DailyProgress]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await DailyProgress.prisma().query_raw(
            'SELECT * FROM DailyProgress WHERE id = ?',
            'bjcdajabfa',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.DailyProgress
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await DailyProgress.prisma().query_first(
            'SELECT * FROM DailyProgress WHERE userId = ?',
            'bchhceeeff',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.DailyProgressCreateInput,
        include: Optional[types.DailyProgressInclude] = None
    ) -> _PrismaModelT:
        """Create a new DailyProgress record.

        Parameters
        ----------
        data
            DailyProgress record data
        include
            Specifies which relations should be loaded on the returned DailyProgress model

        Returns
        -------
        prisma.models.DailyProgress
            The created DailyProgress record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a DailyProgress record from just the required fields
        dailyprogress = await DailyProgress.prisma().create(
            data={
                # data to create a DailyProgress record
                'userId': 'bbgaifhdaa',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.DailyProgressCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple DailyProgress records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of DailyProgress record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await DailyProgress.prisma().create_many(
            data=[
                {
                    # data to create a DailyProgress record
                    'userId': 'dgbcdaegb',
                },
                {
                    # data to create a DailyProgress record
                    'userId': 'beagfbbjig',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.DailyProgressWhereUniqueInput,
        include: Optional[types.DailyProgressInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single DailyProgress record.

        Parameters
        ----------
        where
            DailyProgress filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned DailyProgress model

        Returns
        -------
        prisma.models.DailyProgress
            The deleted DailyProgress record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        dailyprogress = await DailyProgress.prisma().delete(
            where={
                'id': 'beicihhijb',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.DailyProgressWhereUniqueInput,
        include: Optional[types.DailyProgressInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique DailyProgress record.

        Parameters
        ----------
        where
            DailyProgress filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned DailyProgress model

        Returns
        -------
        prisma.models.DailyProgress
            The found DailyProgress record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        dailyprogress = await DailyProgress.prisma().find_unique(
            where={
                'id': 'fgggcdcjg',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.DailyProgressWhereUniqueInput,
        include: Optional[types.DailyProgressInclude] = None
    ) -> _PrismaModelT:
        """Find a unique DailyProgress record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            DailyProgress filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned DailyProgress model

        Returns
        -------
        prisma.models.DailyProgress
            The found DailyProgress record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        dailyprogress = await DailyProgress.prisma().find_unique_or_raise(
            where={
                'id': 'ccjbbjigf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DailyProgressWhereInput] = None,
        cursor: Optional[types.DailyProgressWhereUniqueInput] = None,
        include: Optional[types.DailyProgressInclude] = None,
        order: Optional[Union[types.DailyProgressOrderByInput, List[types.DailyProgressOrderByInput]]] = None,
        distinct: Optional[List[types.DailyProgressScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple DailyProgress records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of DailyProgress records returned
        skip
            Ignore the first N results
        where
            DailyProgress filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned DailyProgress model
        order
            Order the returned DailyProgress records by any field
        distinct
            Filter DailyProgress records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.DailyProgress]
            The list of all DailyProgress records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 DailyProgress records
        dailyprogress = await DailyProgress.prisma().find_many(take=10)

        # find the first 5 DailyProgress records ordered by the date field
        dailyprogress = await DailyProgress.prisma().find_many(
            take=5,
            order={
                'date': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.DailyProgressWhereInput] = None,
        cursor: Optional[types.DailyProgressWhereUniqueInput] = None,
        include: Optional[types.DailyProgressInclude] = None,
        order: Optional[Union[types.DailyProgressOrderByInput, List[types.DailyProgressOrderByInput]]] = None,
        distinct: Optional[List[types.DailyProgressScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single DailyProgress record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            DailyProgress filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned DailyProgress model
        order
            Order the returned DailyProgress records by any field
        distinct
            Filter DailyProgress records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.DailyProgress
            The first DailyProgress record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second DailyProgress record ordered by the readingTime field
        dailyprogress = await DailyProgress.prisma().find_first(
            skip=1,
            order={
                'readingTime': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.DailyProgressWhereInput] = None,
        cursor: Optional[types.DailyProgressWhereUniqueInput] = None,
        include: Optional[types.DailyProgressInclude] = None,
        order: Optional[Union[types.DailyProgressOrderByInput, List[types.DailyProgressOrderByInput]]] = None,
        distinct: Optional[List[types.DailyProgressScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single DailyProgress record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            DailyProgress filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned DailyProgress model
        order
            Order the returned DailyProgress records by any field
        distinct
            Filter DailyProgress records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.DailyProgress
            The first DailyProgress record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second DailyProgress record ordered by the wordsRead field
        dailyprogress = await DailyProgress.prisma().find_first_or_raise(
            skip=1,
            order={
                'wordsRead': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.DailyProgressUpdateInput,
        where: types.DailyProgressWhereUniqueInput,
        include: Optional[types.DailyProgressInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single DailyProgress record.

        Parameters
        ----------
        data
            DailyProgress record data specifying what to update
        where
            DailyProgress filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned DailyProgress model

        Returns
        -------
        prisma.models.DailyProgress
            The updated DailyProgress record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        dailyprogress = await DailyProgress.prisma().update(
            where={
                'id': 'bhfaabbaha',
            },
            data={
                # data to update the DailyProgress record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.DailyProgressWhereUniqueInput,
        data: types.DailyProgressUpsertInput,
        include: Optional[types.DailyProgressInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            DailyProgress filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned DailyProgress model

        Returns
        -------
        prisma.models.DailyProgress
            The created or updated DailyProgress record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        dailyprogress = await DailyProgress.prisma().upsert(
            where={
                'id': 'ebajedhhf',
            },
            data={
                'create': {
                    'id': 'ebajedhhf',
                    'userId': 'beagfbbjig',
                },
                'update': {
                    'userId': 'beagfbbjig',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.DailyProgressUpdateManyMutationInput,
        where: types.DailyProgressWhereInput,
    ) -> int:
        """Update multiple DailyProgress records

        Parameters
        ----------
        data
            DailyProgress data to update the selected DailyProgress records to
        where
            Filter to select the DailyProgress records to update

        Returns
        -------
        int
            The total number of DailyProgress records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all DailyProgress records
        total = await DailyProgress.prisma().update_many(
            data={
                'spellingAccuracy': 909024364.75561
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DailyProgressWhereInput] = None,
        cursor: Optional[types.DailyProgressWhereUniqueInput] = None,
    ) -> int:
        """Count the number of DailyProgress records present in the database

        Parameters
        ----------
        select
            Select the DailyProgress fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            DailyProgress filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.DailyProgressCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await DailyProgress.prisma().count()

        # results: prisma.types.DailyProgressCountAggregateOutput
        results = await DailyProgress.prisma().count(
            select={
                '_all': True,
                'gamesCompleted': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.DailyProgressCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DailyProgressWhereInput] = None,
        cursor: Optional[types.DailyProgressWhereUniqueInput] = None,
    ) -> types.DailyProgressCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.DailyProgressCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DailyProgressWhereInput] = None,
        cursor: Optional[types.DailyProgressWhereUniqueInput] = None,
    ) -> Union[int, types.DailyProgressCountAggregateOutput]:
        """Count the number of DailyProgress records present in the database

        Parameters
        ----------
        select
            Select the DailyProgress fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            DailyProgress filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.DailyProgressCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await DailyProgress.prisma().count()

        # results: prisma.types.DailyProgressCountAggregateOutput
        results = await DailyProgress.prisma().count(
            select={
                '_all': True,
                'studyTime': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.DailyProgressCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.DailyProgressWhereInput] = None
    ) -> int:
        """Delete multiple DailyProgress records.

        Parameters
        ----------
        where
            Optional DailyProgress filter to find the records to be deleted

        Returns
        -------
        int
            The total number of DailyProgress records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all DailyProgress records
        total = await DailyProgress.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.DailyProgressScalarFieldKeys'],
        *,
        where: Optional['types.DailyProgressWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.DailyProgressAvgAggregateInput'] = None,
        sum: Optional['types.DailyProgressSumAggregateInput'] = None,
        min: Optional['types.DailyProgressMinAggregateInput'] = None,
        max: Optional['types.DailyProgressMaxAggregateInput'] = None,
        having: Optional['types.DailyProgressScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.DailyProgressCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.DailyProgressScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.DailyProgressScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.DailyProgressGroupByOutput']:
        """Group DailyProgress records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar DailyProgress fields to group records by
        where
            DailyProgress filter to select records
        take
            Limit the maximum number of DailyProgress records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.DailyProgressGroupByOutput]
            A list of dictionaries representing the DailyProgress record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group DailyProgress records by createdAt values
        # and count how many records are in each group
        results = await DailyProgress.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class SkillMasteryActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.SkillMastery]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await SkillMastery.prisma().query_raw(
            'SELECT * FROM SkillMastery WHERE id = ?',
            'biacbiieja',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.SkillMastery
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await SkillMastery.prisma().query_first(
            'SELECT * FROM SkillMastery WHERE userId = ?',
            'cjejbgbff',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.SkillMasteryCreateInput,
        include: Optional[types.SkillMasteryInclude] = None
    ) -> _PrismaModelT:
        """Create a new SkillMastery record.

        Parameters
        ----------
        data
            SkillMastery record data
        include
            Specifies which relations should be loaded on the returned SkillMastery model

        Returns
        -------
        prisma.models.SkillMastery
            The created SkillMastery record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a SkillMastery record from just the required fields
        skillmastery = await SkillMastery.prisma().create(
            data={
                # data to create a SkillMastery record
                'userId': 'fgeahddae',
                'skillType': 'diageigcf',
                'skillName': 'badagbgeha',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.SkillMasteryCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple SkillMastery records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of SkillMastery record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await SkillMastery.prisma().create_many(
            data=[
                {
                    # data to create a SkillMastery record
                    'userId': 'ibgebbjch',
                    'skillType': 'baieajjiee',
                    'skillName': 'bahjhaccfd',
                },
                {
                    # data to create a SkillMastery record
                    'userId': 'hffhfabhi',
                    'skillType': 'bbcigiadhb',
                    'skillName': 'cfjagbbae',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.SkillMasteryWhereUniqueInput,
        include: Optional[types.SkillMasteryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single SkillMastery record.

        Parameters
        ----------
        where
            SkillMastery filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned SkillMastery model

        Returns
        -------
        prisma.models.SkillMastery
            The deleted SkillMastery record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        skillmastery = await SkillMastery.prisma().delete(
            where={
                'id': 'bbbfhdidef',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.SkillMasteryWhereUniqueInput,
        include: Optional[types.SkillMasteryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique SkillMastery record.

        Parameters
        ----------
        where
            SkillMastery filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned SkillMastery model

        Returns
        -------
        prisma.models.SkillMastery
            The found SkillMastery record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        skillmastery = await SkillMastery.prisma().find_unique(
            where={
                'id': 'bdadhibhec',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.SkillMasteryWhereUniqueInput,
        include: Optional[types.SkillMasteryInclude] = None
    ) -> _PrismaModelT:
        """Find a unique SkillMastery record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            SkillMastery filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned SkillMastery model

        Returns
        -------
        prisma.models.SkillMastery
            The found SkillMastery record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        skillmastery = await SkillMastery.prisma().find_unique_or_raise(
            where={
                'id': 'bfhdjaiejf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SkillMasteryWhereInput] = None,
        cursor: Optional[types.SkillMasteryWhereUniqueInput] = None,
        include: Optional[types.SkillMasteryInclude] = None,
        order: Optional[Union[types.SkillMasteryOrderByInput, List[types.SkillMasteryOrderByInput]]] = None,
        distinct: Optional[List[types.SkillMasteryScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple SkillMastery records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of SkillMastery records returned
        skip
            Ignore the first N results
        where
            SkillMastery filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned SkillMastery model
        order
            Order the returned SkillMastery records by any field
        distinct
            Filter SkillMastery records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.SkillMastery]
            The list of all SkillMastery records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 SkillMastery records
        skillmasterys = await SkillMastery.prisma().find_many(take=10)

        # find the first 5 SkillMastery records ordered by the skillType field
        skillmasterys = await SkillMastery.prisma().find_many(
            take=5,
            order={
                'skillType': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SkillMasteryWhereInput] = None,
        cursor: Optional[types.SkillMasteryWhereUniqueInput] = None,
        include: Optional[types.SkillMasteryInclude] = None,
        order: Optional[Union[types.SkillMasteryOrderByInput, List[types.SkillMasteryOrderByInput]]] = None,
        distinct: Optional[List[types.SkillMasteryScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single SkillMastery record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            SkillMastery filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned SkillMastery model
        order
            Order the returned SkillMastery records by any field
        distinct
            Filter SkillMastery records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.SkillMastery
            The first SkillMastery record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second SkillMastery record ordered by the skillName field
        skillmastery = await SkillMastery.prisma().find_first(
            skip=1,
            order={
                'skillName': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SkillMasteryWhereInput] = None,
        cursor: Optional[types.SkillMasteryWhereUniqueInput] = None,
        include: Optional[types.SkillMasteryInclude] = None,
        order: Optional[Union[types.SkillMasteryOrderByInput, List[types.SkillMasteryOrderByInput]]] = None,
        distinct: Optional[List[types.SkillMasteryScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single SkillMastery record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            SkillMastery filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned SkillMastery model
        order
            Order the returned SkillMastery records by any field
        distinct
            Filter SkillMastery records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.SkillMastery
            The first SkillMastery record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second SkillMastery record ordered by the masteryLevel field
        skillmastery = await SkillMastery.prisma().find_first_or_raise(
            skip=1,
            order={
                'masteryLevel': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.SkillMasteryUpdateInput,
        where: types.SkillMasteryWhereUniqueInput,
        include: Optional[types.SkillMasteryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single SkillMastery record.

        Parameters
        ----------
        data
            SkillMastery record data specifying what to update
        where
            SkillMastery filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned SkillMastery model

        Returns
        -------
        prisma.models.SkillMastery
            The updated SkillMastery record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        skillmastery = await SkillMastery.prisma().update(
            where={
                'id': 'bbjfijjadg',
            },
            data={
                # data to update the SkillMastery record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.SkillMasteryWhereUniqueInput,
        data: types.SkillMasteryUpsertInput,
        include: Optional[types.SkillMasteryInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            SkillMastery filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned SkillMastery model

        Returns
        -------
        prisma.models.SkillMastery
            The created or updated SkillMastery record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        skillmastery = await SkillMastery.prisma().upsert(
            where={
                'id': 'hdjacbehh',
            },
            data={
                'create': {
                    'id': 'hdjacbehh',
                    'userId': 'hffhfabhi',
                    'skillType': 'bbcigiadhb',
                    'skillName': 'cfjagbbae',
                },
                'update': {
                    'userId': 'hffhfabhi',
                    'skillType': 'bbcigiadhb',
                    'skillName': 'cfjagbbae',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.SkillMasteryUpdateManyMutationInput,
        where: types.SkillMasteryWhereInput,
    ) -> int:
        """Update multiple SkillMastery records

        Parameters
        ----------
        data
            SkillMastery data to update the selected SkillMastery records to
        where
            Filter to select the SkillMastery records to update

        Returns
        -------
        int
            The total number of SkillMastery records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all SkillMastery records
        total = await SkillMastery.prisma().update_many(
            data={
                'practicedAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SkillMasteryWhereInput] = None,
        cursor: Optional[types.SkillMasteryWhereUniqueInput] = None,
    ) -> int:
        """Count the number of SkillMastery records present in the database

        Parameters
        ----------
        select
            Select the SkillMastery fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            SkillMastery filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SkillMasteryCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await SkillMastery.prisma().count()

        # results: prisma.types.SkillMasteryCountAggregateOutput
        results = await SkillMastery.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.SkillMasteryCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SkillMasteryWhereInput] = None,
        cursor: Optional[types.SkillMasteryWhereUniqueInput] = None,
    ) -> types.SkillMasteryCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.SkillMasteryCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SkillMasteryWhereInput] = None,
        cursor: Optional[types.SkillMasteryWhereUniqueInput] = None,
    ) -> Union[int, types.SkillMasteryCountAggregateOutput]:
        """Count the number of SkillMastery records present in the database

        Parameters
        ----------
        select
            Select the SkillMastery fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            SkillMastery filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SkillMasteryCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await SkillMastery.prisma().count()

        # results: prisma.types.SkillMasteryCountAggregateOutput
        results = await SkillMastery.prisma().count(
            select={
                '_all': True,
                'updatedAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.SkillMasteryCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.SkillMasteryWhereInput] = None
    ) -> int:
        """Delete multiple SkillMastery records.

        Parameters
        ----------
        where
            Optional SkillMastery filter to find the records to be deleted

        Returns
        -------
        int
            The total number of SkillMastery records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all SkillMastery records
        total = await SkillMastery.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.SkillMasteryScalarFieldKeys'],
        *,
        where: Optional['types.SkillMasteryWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.SkillMasteryAvgAggregateInput'] = None,
        sum: Optional['types.SkillMasterySumAggregateInput'] = None,
        min: Optional['types.SkillMasteryMinAggregateInput'] = None,
        max: Optional['types.SkillMasteryMaxAggregateInput'] = None,
        having: Optional['types.SkillMasteryScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.SkillMasteryCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.SkillMasteryScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.SkillMasteryScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.SkillMasteryGroupByOutput']:
        """Group SkillMastery records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar SkillMastery fields to group records by
        where
            SkillMastery filter to select records
        take
            Limit the maximum number of SkillMastery records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.SkillMasteryGroupByOutput]
            A list of dictionaries representing the SkillMastery record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group SkillMastery records by id values
        # and count how many records are in each group
        results = await SkillMastery.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ReadingCoachSessionActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ReadingCoachSession]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await ReadingCoachSession.prisma().query_raw(
            'SELECT * FROM ReadingCoachSession WHERE id = ?',
            'bhcccbeaba',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ReadingCoachSession
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await ReadingCoachSession.prisma().query_first(
            'SELECT * FROM ReadingCoachSession WHERE userId = ?',
            'bcgjbdgjdj',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ReadingCoachSessionCreateInput,
        include: Optional[types.ReadingCoachSessionInclude] = None
    ) -> _PrismaModelT:
        """Create a new ReadingCoachSession record.

        Parameters
        ----------
        data
            ReadingCoachSession record data
        include
            Specifies which relations should be loaded on the returned ReadingCoachSession model

        Returns
        -------
        prisma.models.ReadingCoachSession
            The created ReadingCoachSession record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ReadingCoachSession record from just the required fields
        readingcoachsession = await ReadingCoachSession.prisma().create(
            data={
                # data to create a ReadingCoachSession record
                'userId': 'fhdbhifae',
                'passageText': 'beeacgfcej',
                'spokenText': 'bbifhdiicc',
                'accuracyScore': 1694224903.190636,
                'wordsCorrect': 1911255389,
                'wordsIncorrect': 146262738,
                'errors': 'bhbjceagbb',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ReadingCoachSessionCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ReadingCoachSession records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ReadingCoachSession record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await ReadingCoachSession.prisma().create_many(
            data=[
                {
                    # data to create a ReadingCoachSession record
                    'userId': 'bjeifffjdg',
                    'passageText': 'bdidcfdfjd',
                    'spokenText': 'dfeggejja',
                    'accuracyScore': 647166719.35708,
                    'wordsCorrect': 612396821,
                    'wordsIncorrect': 1808130602,
                    'errors': 'bbfbheibcd',
                },
                {
                    # data to create a ReadingCoachSession record
                    'userId': 'hiagajie',
                    'passageText': 'eeejidbif',
                    'spokenText': 'efgbahec',
                    'accuracyScore': 769084151.133459,
                    'wordsCorrect': 1116761037,
                    'wordsIncorrect': 1172606082,
                    'errors': 'ddaabegbb',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ReadingCoachSessionWhereUniqueInput,
        include: Optional[types.ReadingCoachSessionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ReadingCoachSession record.

        Parameters
        ----------
        where
            ReadingCoachSession filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ReadingCoachSession model

        Returns
        -------
        prisma.models.ReadingCoachSession
            The deleted ReadingCoachSession record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        readingcoachsession = await ReadingCoachSession.prisma().delete(
            where={
                'id': 'bhgibfgbbc',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ReadingCoachSessionWhereUniqueInput,
        include: Optional[types.ReadingCoachSessionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ReadingCoachSession record.

        Parameters
        ----------
        where
            ReadingCoachSession filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ReadingCoachSession model

        Returns
        -------
        prisma.models.ReadingCoachSession
            The found ReadingCoachSession record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        readingcoachsession = await ReadingCoachSession.prisma().find_unique(
            where={
                'id': 'hbgcihef',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ReadingCoachSessionWhereUniqueInput,
        include: Optional[types.ReadingCoachSessionInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ReadingCoachSession record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ReadingCoachSession filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ReadingCoachSession model

        Returns
        -------
        prisma.models.ReadingCoachSession
            The found ReadingCoachSession record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        readingcoachsession = await ReadingCoachSession.prisma().find_unique_or_raise(
            where={
                'id': 'ffhgghde',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReadingCoachSessionWhereInput] = None,
        cursor: Optional[types.ReadingCoachSessionWhereUniqueInput] = None,
        include: Optional[types.ReadingCoachSessionInclude] = None,
        order: Optional[Union[types.ReadingCoachSessionOrderByInput, List[types.ReadingCoachSessionOrderByInput]]] = None,
        distinct: Optional[List[types.ReadingCoachSessionScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ReadingCoachSession records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ReadingCoachSession records returned
        skip
            Ignore the first N results
        where
            ReadingCoachSession filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ReadingCoachSession model
        order
            Order the returned ReadingCoachSession records by any field
        distinct
            Filter ReadingCoachSession records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ReadingCoachSession]
            The list of all ReadingCoachSession records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ReadingCoachSession records
        readingcoachsessions = await ReadingCoachSession.prisma().find_many(take=10)

        # find the first 5 ReadingCoachSession records ordered by the passageText field
        readingcoachsessions = await ReadingCoachSession.prisma().find_many(
            take=5,
            order={
                'passageText': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ReadingCoachSessionWhereInput] = None,
        cursor: Optional[types.ReadingCoachSessionWhereUniqueInput] = None,
        include: Optional[types.ReadingCoachSessionInclude] = None,
        order: Optional[Union[types.ReadingCoachSessionOrderByInput, List[types.ReadingCoachSessionOrderByInput]]] = None,
        distinct: Optional[List[types.ReadingCoachSessionScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ReadingCoachSession record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ReadingCoachSession filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ReadingCoachSession model
        order
            Order the returned ReadingCoachSession records by any field
        distinct
            Filter ReadingCoachSession records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ReadingCoachSession
            The first ReadingCoachSession record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ReadingCoachSession record ordered by the spokenText field
        readingcoachsession = await ReadingCoachSession.prisma().find_first(
            skip=1,
            order={
                'spokenText': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ReadingCoachSessionWhereInput] = None,
        cursor: Optional[types.ReadingCoachSessionWhereUniqueInput] = None,
        include: Optional[types.ReadingCoachSessionInclude] = None,
        order: Optional[Union[types.ReadingCoachSessionOrderByInput, List[types.ReadingCoachSessionOrderByInput]]] = None,
        distinct: Optional[List[types.ReadingCoachSessionScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ReadingCoachSession record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ReadingCoachSession filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ReadingCoachSession model
        order
            Order the returned ReadingCoachSession records by any field
        distinct
            Filter ReadingCoachSession records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ReadingCoachSession
            The first ReadingCoachSession record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ReadingCoachSession record ordered by the accuracyScore field
        readingcoachsession = await ReadingCoachSession.prisma().find_first_or_raise(
            skip=1,
            order={
                'accuracyScore': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ReadingCoachSessionUpdateInput,
        where: types.ReadingCoachSessionWhereUniqueInput,
        include: Optional[types.ReadingCoachSessionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ReadingCoachSession record.

        Parameters
        ----------
        data
            ReadingCoachSession record data specifying what to update
        where
            ReadingCoachSession filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ReadingCoachSession model

        Returns
        -------
        prisma.models.ReadingCoachSession
            The updated ReadingCoachSession record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        readingcoachsession = await ReadingCoachSession.prisma().update(
            where={
                'id': 'ibcadcejf',
            },
            data={
                # data to update the ReadingCoachSession record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ReadingCoachSessionWhereUniqueInput,
        data: types.ReadingCoachSessionUpsertInput,
        include: Optional[types.ReadingCoachSessionInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ReadingCoachSession filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ReadingCoachSession model

        Returns
        -------
        prisma.models.ReadingCoachSession
            The created or updated ReadingCoachSession record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        readingcoachsession = await ReadingCoachSession.prisma().upsert(
            where={
                'id': 'bdcdfgccdg',
            },
            data={
                'create': {
                    'id': 'bdcdfgccdg',
                    'userId': 'hiagajie',
                    'passageText': 'eeejidbif',
                    'spokenText': 'efgbahec',
                    'accuracyScore': 769084151.133459,
                    'wordsCorrect': 1116761037,
                    'wordsIncorrect': 1172606082,
                    'errors': 'ddaabegbb',
                },
                'update': {
                    'userId': 'hiagajie',
                    'passageText': 'eeejidbif',
                    'spokenText': 'efgbahec',
                    'accuracyScore': 769084151.133459,
                    'wordsCorrect': 1116761037,
                    'wordsIncorrect': 1172606082,
                    'errors': 'ddaabegbb',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ReadingCoachSessionUpdateManyMutationInput,
        where: types.ReadingCoachSessionWhereInput,
    ) -> int:
        """Update multiple ReadingCoachSession records

        Parameters
        ----------
        data
            ReadingCoachSession data to update the selected ReadingCoachSession records to
        where
            Filter to select the ReadingCoachSession records to update

        Returns
        -------
        int
            The total number of ReadingCoachSession records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ReadingCoachSession records
        total = await ReadingCoachSession.prisma().update_many(
            data={
                'wordsCorrect': 43796357
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReadingCoachSessionWhereInput] = None,
        cursor: Optional[types.ReadingCoachSessionWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ReadingCoachSession records present in the database

        Parameters
        ----------
        select
            Select the ReadingCoachSession fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ReadingCoachSession filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ReadingCoachSessionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ReadingCoachSession.prisma().count()

        # results: prisma.types.ReadingCoachSessionCountAggregateOutput
        results = await ReadingCoachSession.prisma().count(
            select={
                '_all': True,
                'wordsIncorrect': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ReadingCoachSessionCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReadingCoachSessionWhereInput] = None,
        cursor: Optional[types.ReadingCoachSessionWhereUniqueInput] = None,
    ) -> types.ReadingCoachSessionCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ReadingCoachSessionCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ReadingCoachSessionWhereInput] = None,
        cursor: Optional[types.ReadingCoachSessionWhereUniqueInput] = None,
    ) -> Union[int, types.ReadingCoachSessionCountAggregateOutput]:
        """Count the number of ReadingCoachSession records present in the database

        Parameters
        ----------
        select
            Select the ReadingCoachSession fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ReadingCoachSession filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ReadingCoachSessionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ReadingCoachSession.prisma().count()

        # results: prisma.types.ReadingCoachSessionCountAggregateOutput
        results = await ReadingCoachSession.prisma().count(
            select={
                '_all': True,
                'errors': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ReadingCoachSessionCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ReadingCoachSessionWhereInput] = None
    ) -> int:
        """Delete multiple ReadingCoachSession records.

        Parameters
        ----------
        where
            Optional ReadingCoachSession filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ReadingCoachSession records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ReadingCoachSession records
        total = await ReadingCoachSession.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ReadingCoachSessionScalarFieldKeys'],
        *,
        where: Optional['types.ReadingCoachSessionWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ReadingCoachSessionAvgAggregateInput'] = None,
        sum: Optional['types.ReadingCoachSessionSumAggregateInput'] = None,
        min: Optional['types.ReadingCoachSessionMinAggregateInput'] = None,
        max: Optional['types.ReadingCoachSessionMaxAggregateInput'] = None,
        having: Optional['types.ReadingCoachSessionScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ReadingCoachSessionCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ReadingCoachSessionScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ReadingCoachSessionScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ReadingCoachSessionGroupByOutput']:
        """Group ReadingCoachSession records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ReadingCoachSession fields to group records by
        where
            ReadingCoachSession filter to select records
        take
            Limit the maximum number of ReadingCoachSession records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ReadingCoachSessionGroupByOutput]
            A list of dictionaries representing the ReadingCoachSession record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ReadingCoachSession records by phoneticFeedback values
        # and count how many records are in each group
        results = await ReadingCoachSession.prisma().group_by(
            ['phoneticFeedback'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models